# Architecture Notes

Source of truth: `Ref/system_architecture.mermaid` + `Ref/hybrid_C_resolution_flow.mermaid`. This doc is the prose companion.

## 1. System Architecture (4 parts)

### 1.1 Capture — one user-scope hook, every session (v0.1.1.0)

- `Edit` / `Write` / `MultiEdit` fires a **PostToolUse hook** — a tiny Python script registered ONCE per PC at user scope (`C:\Users\ADMIN\.claude\settings.json`), so it fires in EVERY Claude Code session regardless of the session's root (the real workflow is one session spanning several repos)
- The hook routes each event to the repo that OWNS the edited file (nearest `.git` ancestor) and appends a raw event `{file, timestamp, tool}` to that repo's `events.jsonl`
- **Allowlist**: only repos registered in `Config/repos.yaml` are captured (fail-open to capture-all when the registry is unreadable/empty; path lines must stay single-line + single-quoted — the hook reads the registry with a stdlib regex, not YAML)
- **Durable by design**: pure file append — capture keeps working even when the server is down

### 1.2 Data sources (per monitored repo)

| Source | Role |
|---|---|
| `events.jsonl` | raw change events from the hook |
| PLAN files (task blocks) | the "why" — tasks, file declarations, statuses |
| `.git` (logs/HEAD, index) | truth for committed vs. uncommitted, diffs, commit detection |

### 1.3 Local server — FastAPI, single process

- **File watchers** (`watchfiles`): tail `events.jsonl`, watch plan files (parse + cache, re-parse on change)
- **Resolver**: hybrid-C decision tree (section 2) attributes each event to a task
- **Git module**: status · diff · commit detection per repo
- **SQLite**: events · tasks · commits
- **API**: REST + WebSocket (live push to UI)

### 1.4 Browser UI — React + Vite + Tailwind (English)

- **Status bar**: `CLEAN ✓` / `N uncommitted changes` (resets to CLEAN when all changes are committed)
- **Task sidebar**: plan tasks + status chips
- **Main view**: changes grouped by task — why + files + diff viewer + **AMBIGUOUS queue** (manual pick)
- **Overview tab** (v0.1.3.0/v0.1.4.0): KPI metric cards + Chart.js dashboard (attribution doughnut · events-per-task bar · 14-day activity line) + lazy Mermaid task→commit relationship map
- **History tab**: commit → tasks → events (commits counted separately)

## 2. Hybrid-C Resolution (the "why" attribution)

For every event, match the changed file path against ALL tasks' file-pattern declarations → N matches:

| Case | Rule | Result |
|---|---|---|
| N = 1 | declaration wins | **MODE B** — tag with that task |
| N > 1 (shared file) | exactly 1 of the matches is `in-progress` | **MODE A scoped** — tag with that one |
| N > 1 | 0 or ≥2 in-progress among matches | **AMBIGUOUS** → UI manual-pick queue |
| N = 0 (undeclared file) | exactly 1 task in-progress in the WHOLE plan | **MODE A global fallback** |
| N = 0 | 0 or ≥2 in-progress overall | **UNKNOWN** → UI manual-pick queue |

**Key principles:**

1. **B first, A as tie-breaker/fallback** — file declarations beat status; status disambiguates
2. **NEVER silently guess** — uncertain events go to the manual-pick queue, not a wrong "why"
3. Resolved events persist to SQLite and push live to the UI via WebSocket

## 2b. As-Built Notes (v0.1.0.0, 2026-07-07)

Implemented per the reviewed plan (57 findings baked in) + post-implementation CFT fixes CFT-1..CFT-12 (see plan Status log). Key as-built mappings: capture = `Hook/katlab_tracking_hook.py`; server = `Backend/app/` (config → db → plan_parser → resolver → git_module → watcher → api/routes + api/ws → main); UI = `Frontend/src/` (built to `Frontend/dist`, served by FastAPI). Startup order is FIXED (CFT-12 as-built): config → DB → per repo: tracking dir → parse ALL plans → catch-up ingest → commit backfill → initial git status → startup sweep (if CLEAN) → then watchers (events/plans watcher + dedicated `.git/logs` watcher per repo — watchfiles' default filter ignores `.git`) + poll loop. Status refresh: ingest + `.git/logs` change + poll (`status_poll_seconds`) + on `/api/repos`. v0.1.1.0 (2026-07-12): hook registration moved to USER scope + registry allowlist inside the hook (per-repo registration RETIRED — it never fired in the real 1-session-N-repos workflow); UM_Dev registered as the 3rd monitored repo. v0.1.2.0 (2026-07-13): UI/UX pass — human badge terms + Legend, commit-aware task states, context-aware diff (`/api/diff?commit=` via `show --format=`; empty-diff classification via `status --porcelain --ignored`, prefix-parsed), sidebar plan-grouping + click-filter, local timestamps + 60s tick, bulk manual-pick, plan-edit collapse, `last_event_ts` heartbeat in `/api/repos`, favicon. v0.1.3.0 (2026-07-14): visualization — colored diff, Chart.js Overview dashboard behind ONE additive read-only endpoint `/api/stats` (zero-filled fixed shape: all 6 modes, 14 UTC days) + `activity_buckets` on `/api/repos` (60-min sparkline), lazy-loaded Mermaid task→commit map (≤12-node cap, its own Vite chunk), `theme.ts` MODE_COLOR as the single color source; no schema change, no new git calls. v0.1.4.0 (2026-07-17): visual polish, UI-only (sole Backend touch = the version constant) — "Plus Jakarta Sans" + "Azeret Mono" typography wired into Tailwind + Mermaid + Chart.js, 6 KPI cards (needs-a-pick = pick-queue N invariant), Mermaid viewport shell (zoom/pan/fullscreen-expand, DOMParser+adoptNode injection), changed-files tree with churn badges, sticky scroll-spy nav, once-per-session staggered reveal (reduced-motion safe), background atmosphere. v0.1.5.0 (2026-07-19): insights & awareness — the hook captures `session_id` (EVENT VERSION stays 1, optional key, upgrade-order-free; non-empty-str-or-None guard at both ends), `events.session_id TEXT` via idempotent PRAGMA→ALTER in `init_db`, `activity_calendar` (365 UTC days, events+commits) on `/api/stats` in BOTH `get_stats` return paths; UI adds session dots + session filter, a Sunday-first SVG calendar card, the discipline guard (chip + live queue-lander banner), the attention bell (one bell; OS-alerts toggle in its footer), hidden-tab OS notifications (`notify.ts`), the daily digest export (`digest.ts`), and the Ctrl+K palette (`CommandPalette.tsx`); MODE_BADGE lifted to `theme.ts`; zero new deps, no new endpoints, no new git calls. Post-implementation CFT loop fixes baked in (CFT-1..CFT-9, 14 passes ending 5/5 clean — see the v0.1.5.0 plan CFT log): ingest validates the WHOLE event-line shape (non-dict lines and non-string ts/file warn+skip, tool coerces — never-stall for ANY hand-crafted line, both ends), state-based deferred pick-queue scroll, demo session-id short-8 parity, calendar/digest polish. v0.1.6.0 (2026-07-19): effort & context — the hook also captures `branch` (stdlib `.git/HEAD` read, worktree-aware, fail-open; event version STILL 1), `events.branch` via a generalized needed-columns migration loop; effort estimates from capture timestamps (GAP 15min/TAIL 2min block clustering, ONE algorithm home in `db.py`, mirrored display constants in `theme.ts`): `/api/stats` gains `effort_per_task` + calendar-day `minutes` (fixed shape both paths); `/api/repos` gains `branch` (per-field keep-last-known in the status dict) + `oldest_uncommitted_ts`; `/api/tasks` gains `last_event_ts`; `/api/events` gains the optional `session` filter; UI: stats fetch lifted OverviewView→App, effort lines on sidebar/groups, "time today (UTC)" KPI, `⎇` branch chips + differs-suffix, SessionTimeline modal (static snapshot), bell rhythm nudges (idle 24h / uncommitted-age 48h); zero new deps, no new endpoints, one extra read-only rev-parse per status refresh (two while HEAD is detached — the short-hash fallback). v0.1.7.0 (2026-07-20): story & delight — `/api/stats` gains `file_coupling` (per-task DISTINCT-triple pairing, plan files excluded via `tasks.plan_file`, >30-file mega-tasks skipped, top-10 deterministic order) and `punch_card` (7×24 Sunday-first SERVER-LOCAL counts via `(weekday()+1)%7`), both fixed-shape in BOTH `get_stats` return paths; `/api/events` gains the optional `file` filter; UI: Overview coupling card (hidden at 0 pairs) + pure-SVG punch card (`punchCard.tsx`, RAMP imported from the calendar — single ramp source), `FileStory.tsx` modal (Changes-group file names + coupling rows → one file's chronological life; static snapshot, UTC day separators, mirrored-constant header effort), `streakOf()` calendar-header streak (UTC, today-zero grace, ≥2 only), session pulse (render-derived `last_event_ts < 5min` breathing dot), CLEAN ✓ celebration per the channel matrix (persistent per-repo toast z-30 always + nonce-keyed ~12-particle chip burst while visible + unchanged hidden-only OS alert; `notifyStatusChange` returns the transition), View Transitions crossfade on nav switches only (`withViewTransition` = feature-detect + reduced-motion guard + flushSync); NO schema change (hook + event version + DB untouched), zero new deps, no new endpoints, no new git calls. v0.1.7.1 (2026-07-21): F58 sweep hotfix — `_poll_loop` gains an unconditional safety-net sweep outside the dirty→clean transition gate (every poll: CLEAN repo + unlinked events → sweep to HEAD, `swept=1`); closes the gitignored-plan-edit gap where an event accrues without any git-status change and no sweep trigger ever fires (ordering-correct: `_detect_commits` links real commits first in the same iteration; idempotent; offline-safe). v0.1.8.0 (2026-07-22): replay & recap — `/api/events` gains optional `since`/`until` (ISO-Z TEXT window, composes with every filter); `/api/stats` gains `wrapped` (last-7-UTC-days story: calendar-projected days, top_task/busiest_hour/top_pair with deterministic tie-breaks, files_touched incl. plan files, window commits; fixed shape both paths via `_empty_wrapped`); UI: `dayLanes.tsx` (24h local lanes from fetched rows' repo_ids, mirrored-constant clusters, day picker, repo-scoped alive-flagged ≤3-page fetch, today-only stats-driven refresh) + the in-card replay engine (rAF virtual time from clamped dt, cursor, 8-chip feed, scrubber, 1x/2x/4x; exits on day/scope change, rAF cancelled on unmount) + `WrappedCard.tsx` ("Your week ✨" modal, init-once snapshot, client-resolved title, exported punchCard DAYS); the planned KPI count-up was DROPPED as already shipped in v0.1.4.0 (IMPL-1); NO schema change, zero new deps, no new endpoints, no new git calls. v0.1.9.0 (2026-07-26): skyline & arcade — `commit_info` format gains `%P` (the ONE `git show` call; zero new git invocations; "" = root, distinct from pre-upgrade NULL), `commits.parents TEXT` via the needed-columns migration loop generalized to a per-table map (NULL persists only beyond the last-20 backfill window, which self-heals on the first restart), `/api/history` rows flow the field free; UI: `skyline.tsx` (pure-SVG 2:1 dimetric city of the 365-day calendar — sqrt heights, back-to-front painter order, shared `rampBucket` lifted from calendarHeatmap, flat-identical tooltips) behind a persisted `flat | city` card toggle (default city) with the legend lifted to one exported `<RampLegend>`; `trophies.tsx` (7 ranked trophies —/C..SSS + 2 secrets, frontend-only, scope-aware); `comboMeter.tsx` + App WS combo (refs mirrored to state, mirrored GAP decay, milestones {5,10,25,50,100,250} with the celebration burst recipe whole incl. the ~900ms nonce-compare clear, live-only); `mermaidGraph.ts` gains `buildGitGraph`/`renderGitGraph` on the ONE initialize (gitGraph themeVariables added; sanitized `mainBranchName`; merges decorate solely with "*"-suffixed side tips + M's id on the merge statement; octopus #3+ list-only; cap 20) and History gains the "⎇ graph" expand card (GraphPanel effect recipe verbatim: alive flag + inline error note + busy; DOMParser+adoptNode; mermaid chunk still first-expand lazy); ONE additive schema column + one additive `/api/history` field, no new endpoints, no new git calls, zero new deps. v0.1.10.0 (2026-07-27): identity & focus — `/api/stats` gains `identity` (top-8 basename extensions + ext_total + sessions + first_event_ts + ALL-TIME commits) and `file_churn` (top-20 by events, with last_ts), BOTH fed by ONE new file-level GROUP BY with the Amendment-A1 DOUBLE plan-file exclusion (`_is_plan_file`: tasks set OR the documented PLAN_*.txt basename — retrofitted into the shipped coupling's both call sites, so task-less plans leave coupling/top_pair/identity/churn while events_per_task + wrapped.files_touched stay deliberately inclusive), fixed shapes both paths; UI: `identityCard.tsx` (stroke-dasharray donut + facts line), `churnMap.tsx` (hand-rolled Bruls squarify, fixed 400x180 viewBox, recency-band fills via the shared RAMP, tiles open FileStory), the new identity|churn 2-col Overview row, and `focusMode.tsx` (z-50 ambient wall: live hero, minute clock, scope-snapshotted "in flight" feed, pulse, effort/streak line, Skyline strip; scope snapshot + focus trap on ✕ + the GraphShell open-effect recipe; palette entry + Legend line; Esc/✕ only); no schema change, no new endpoints, no new git calls, zero new deps.

## 3. Multi-Repo Design

- The server tracks **N repos simultaneously** — each with its own `events.jsonl`, plan files, and `.git`
- Repo registry: see [Monitored_Repos.md](Monitored_Repos.md)
- Divide-and-conquer: hook + guideline are authored HERE; the hook is registered ONCE at user scope (maintained here, v0.1.1.0); each monitored repo self-installs only gitignore + plan rules per the **Installation Guideline** — no logic duplication across repos; `Config/repos.yaml` doubles as the capture allowlist

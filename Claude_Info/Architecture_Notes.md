# Architecture Notes

Current source of truth is the implementation plus normative contracts in `Docs/`.
Historical notes reference `Ref/system_architecture.mermaid` and
`Ref/hybrid_C_resolution_flow.mermaid`, but `Ref/` is absent in this checkout.

## 1. System Architecture (v0.3.0.0)

### 1.1 Capture — user-scope provider hooks, every session

- Claude Code and Codex register the shared Python router once per PC at user scope;
  provider-specific adapters normalize supported lifecycle, tool, and subagent events.
- Successful file tools route one attribution event to the owning registered repo's
  `events.jsonl`; metadata-only provider/check events use the central atomic inbox.
- **Allowlist**: only repos registered in `Config/repos.yaml` are captured. Missing,
  unreadable, empty, invalid, or ambiguous configuration yields a capture no-op while
  provider work continues; path lines stay single-line and single-quoted.
- **Durable by design**: file and check evidence is synchronous and flushed; general
  activity is best-effort asynchronous where supported and works while the server is down.

### 1.2 Data sources (per monitored repo)

| Source | Role |
|---|---|
| `events.jsonl` | bounded legacy file-attribution metadata from the hook |
| central activity inbox | atomic provider lifecycle/tool/check metadata |
| PLAN files (task blocks) | the "why" — tasks, file declarations, statuses |
| `Config/checks.json` | trusted automatic/manual check definitions |
| `.git` (logs/HEAD, index) | truth for committed vs. uncommitted, diffs, commit detection |

### 1.3 Local server — FastAPI, single process

- **Watchers** (`watchfiles`): tail file events, plans, Git logs, and the central activity inbox
- **Resolver**: hybrid-C decision tree (section 2) attributes each event to a task
- **Git module**: bounded read-only `status` · `diff` · `log` · `show` only
- **SQLite**: events · plan snapshots · tasks · requirements · activity/evidence · assignments · commits
- **Readiness**: one backend engine computes evidence freshness, blockers, and seven Mission states
- **API**: bounded REST snapshots + additive WebSocket invalidation signals

### 1.4 Browser UI — React + Vite + Tailwind (English)

- **Responsive shell**: repo scope, six-view navigation, status rail, Attention, and utilities remain reachable at phone, tablet, and desktop widths; Tasks is a modal drawer below 1024px and a persistent sidebar from 1024px.
- **Changes**: changes grouped by task — why + files + diff viewer + **AMBIGUOUS queue** (manual pick), with bounded semantic collections.
- **Mission**: plan readiness, requirement rail, blocker/evidence queue, provider-aware session flight recorder, replay, and exact-data table; all decisions come from the backend.
- **Overview**: Now (KPIs, plans, momentum) → Trends (three Chart.js charts, calendar, day lanes, coupling, punch card) → Explore (goals, identity, churn, trophies, records, provenance) → Relationships (lazy Mermaid). Attribution is a horizontal bar, not the retired doughnut.
- **History**: commit → tasks → events, with independent API fetch depth and visible 50-row paging; commits remain counted separately.
- **City and Chronicle**: City is a paged six-district SVG view with complete-model calculations and an exact-data alternative. Chronicle remains a same-origin iframe whose React host owns sizing/fallback only.

### 1.5 Interface invariants (v0.3.0.0)

- [UI_Design_System.md](../Docs/UI_Design_System.md) is the normative interface contract; semantic tokens and shared primitives live in `Frontend/src/index.css`, `tailwind.config.js`, `ui.tsx`, `icons.tsx`, and `dialog.tsx`.
- The route is `{scope, view}` with canonical `view` then `repo` query order. Workspace scope and a repo literally named `ALL` are distinct. Back/Forward uses opaque entry ids plus in-memory, entry-local UI snapshots; private paths, filters, drafts, results, and Blobs never enter the URL or `history.state`.
- Every modal/drawer uses the portal-based `DialogShell`: one overlay lease, inert background, body/app scroll freeze, dynamic Tab containment, Escape, and safe focus return. Header disclosure-to-dialog handoff returns to the persistent trigger.
- Any semantic collection that can exceed 50 uses the shared bounded pager. Calculations, filtering, mutations, exports, and graph inputs continue to use the complete model; City alone pages six districts.
- User-triggered REST work owns one absolute 10-second deadline plus abort/stale-generation guards. Digest preparation is two-stage and scope-bound; direct report and City downloads stay within their initiating activation.
- One reactive reduced-motion source coordinates CSS, rAF, charts, reveal work, View Transitions, attract mode, and particles. Mission, Overview, and City are route-lazy; Mermaid remains nested-lazy; Mission is excluded from attract mode and the network-only service worker is unchanged.

### 1.6 Mission, evidence, and privacy invariants (v0.3.0.0)

- Plans may declare a top-level `<verification>` block. Raw plan bytes define the
  plan revision; edits stale prior bound evidence while unchanged restart does not.
- Evidence records only bounded structural metadata. Prompts, responses, commands,
  patches, source, transcripts, environment values, stdout/stderr, and error text
  never enter the ledger, API, WebSocket, or DOM.
- Automatic evidence attaches only through one uniquely eligible current plan;
  ambiguity remains visibly unassigned. Manual reassignment is append-only.
- Readiness has seven states: `not_configured`, `planning`, `implementation`,
  `verification`, `blocked`, `ready_to_commit`, and `verified_committed`.
- A passing gate means only that declared evidence is current. Mission does not run
  checks or mutate Git; neither it nor the recorder can commit, push, check out,
  create, or delete branches.
- Sessions use `(provider, session_id)` and plans use `(repo_id, plan_file)`; neither
  raw identifier is globally unique alone.
- Startup globally loads plans, legacy file events, and central activity before
  commit/status reconciliation and readiness, then starts watchers. With persisted
  commit history, reconciliation snapshots HEAD, pages to a known boundary, then
  pages the exact boundary-to-snapshot range so merged sibling commits are included;
  every unseen commit links oldest-first before any clean sweep. An empty history
  seeds only one bounded page. A failed or moving-HEAD scan leaves events unlinked,
  and sweep authority is valid only while HEAD still equals that reconciled snapshot.
  Demo static status is triple-gated and receives no Git probe or Git watcher.
- All-scope Mission reads rebuild authoritative readiness globally. Repo-scoped reads
  and live invalidations recompute only affected repositories and atomically replace
  those cached plan slices; summary counts are then derived from the merged cache.

## 2. Hybrid-C Resolution (the "why" attribution)

For every event, match the changed file path against ALL tasks' file-pattern declarations → N matches:

| Case | Rule | Result |
|---|---|---|
| N = 1 | declaration wins | **MODE B** — tag with that task |
| N > 1 (shared file) | exactly 1 of the matches is `in-progress` | **MODE A scoped** — tag with that one |
| N > 1 | 0 or ≥2 in-progress among matches | **AMBIGUOUS** → UI manual-pick queue |
| N = 0 (undeclared file) | exactly 1 task in-progress in the WHOLE plan | **MODE A global fallback** |
| N = 0 | 0 or ≥2 in-progress overall | **UNKNOWN** → UI manual-pick queue |

**Key principles:**

1. **B first, A as tie-breaker/fallback** — file declarations beat status; status disambiguates
2. **NEVER silently guess** — uncertain events go to the manual-pick queue, not a wrong "why"
3. Resolved events persist to SQLite and push live to the UI via WebSocket

## 2b. As-Built Notes (v0.1.0.0, 2026-07-07)

Implemented per the reviewed plan (57 findings baked in) + post-implementation CFT fixes CFT-1..CFT-12 (see plan Status log). Key as-built mappings: capture = `Hook/katlab_tracking_hook.py`; server = `Backend/app/` (config → db → plan_parser → resolver → git_module → watcher → api/routes + api/ws → main); UI = `Frontend/src/` (built to `Frontend/dist`, served by FastAPI). Startup order is FIXED (CFT-12 as-built): config → DB → per repo: tracking dir → parse ALL plans → catch-up ingest → commit backfill → initial git status → startup sweep (if CLEAN) → then watchers (events/plans watcher + dedicated `.git/logs` watcher per repo — watchfiles' default filter ignores `.git`) + poll loop. Status refresh: ingest + `.git/logs` change + poll (`status_poll_seconds`) + on `/api/repos`. v0.1.1.0 (2026-07-12): hook registration moved to USER scope + registry allowlist inside the hook (per-repo registration RETIRED — it never fired in the real 1-session-N-repos workflow); UM_Dev registered as the 3rd monitored repo. v0.1.2.0 (2026-07-13): UI/UX pass — human badge terms + Legend, commit-aware task states, context-aware diff (`/api/diff?commit=` via `show --format=`; empty-diff classification via `status --porcelain --ignored`, prefix-parsed), sidebar plan-grouping + click-filter, local timestamps + 60s tick, bulk manual-pick, plan-edit collapse, `last_event_ts` heartbeat in `/api/repos`, favicon. v0.1.3.0 (2026-07-14): visualization — colored diff, Chart.js Overview dashboard behind ONE additive read-only endpoint `/api/stats` (zero-filled fixed shape: all 6 modes, 14 UTC days) + `activity_buckets` on `/api/repos` (60-min sparkline), lazy-loaded Mermaid task→commit map (≤12-node cap, its own Vite chunk), `theme.ts` MODE_COLOR as the single color source; no schema change, no new git calls. v0.1.4.0 (2026-07-17): visual polish, UI-only (sole Backend touch = the version constant) — "Plus Jakarta Sans" + "Azeret Mono" typography wired into Tailwind + Mermaid + Chart.js, 6 KPI cards (needs-a-pick = pick-queue N invariant), Mermaid viewport shell (zoom/pan/fullscreen-expand, DOMParser+adoptNode injection), changed-files tree with churn badges, sticky scroll-spy nav, once-per-session staggered reveal (reduced-motion safe), background atmosphere. v0.1.5.0 (2026-07-19): insights & awareness — the hook captures `session_id` (EVENT VERSION stays 1, optional key, upgrade-order-free; non-empty-str-or-None guard at both ends), `events.session_id TEXT` via idempotent PRAGMA→ALTER in `init_db`, `activity_calendar` (365 UTC days, events+commits) on `/api/stats` in BOTH `get_stats` return paths; UI adds session dots + session filter, a Sunday-first SVG calendar card, the discipline guard (chip + live queue-lander banner), the attention bell (one bell; OS-alerts toggle in its footer), hidden-tab OS notifications (`notify.ts`), the daily digest export (`digest.ts`), and the Ctrl+K palette (`CommandPalette.tsx`); MODE_BADGE lifted to `theme.ts`; zero new deps, no new endpoints, no new git calls. Post-implementation CFT loop fixes baked in (CFT-1..CFT-9, 14 passes ending 5/5 clean — see the v0.1.5.0 plan CFT log): ingest validates the WHOLE event-line shape (non-dict lines and non-string ts/file warn+skip, tool coerces — never-stall for ANY hand-crafted line, both ends), state-based deferred pick-queue scroll, demo session-id short-8 parity, calendar/digest polish. v0.1.6.0 (2026-07-19): effort & context — the hook also captures `branch` (stdlib `.git/HEAD` read, worktree-aware, fail-open; event version STILL 1), `events.branch` via a generalized needed-columns migration loop; effort estimates from capture timestamps (GAP 15min/TAIL 2min block clustering, ONE algorithm home in `db.py`, mirrored display constants in `theme.ts`): `/api/stats` gains `effort_per_task` + calendar-day `minutes` (fixed shape both paths); `/api/repos` gains `branch` (per-field keep-last-known in the status dict) + `oldest_uncommitted_ts`; `/api/tasks` gains `last_event_ts`; `/api/events` gains the optional `session` filter; UI: stats fetch lifted OverviewView→App, effort lines on sidebar/groups, "time today (UTC)" KPI, `⎇` branch chips + differs-suffix, SessionTimeline modal (static snapshot), bell rhythm nudges (idle 24h / uncommitted-age 48h); zero new deps, no new endpoints, one extra read-only rev-parse per status refresh (two while HEAD is detached — the short-hash fallback). v0.1.7.0 (2026-07-20): story & delight — `/api/stats` gains `file_coupling` (per-task DISTINCT-triple pairing, plan files excluded via `tasks.plan_file`, >30-file mega-tasks skipped, top-10 deterministic order) and `punch_card` (7×24 Sunday-first SERVER-LOCAL counts via `(weekday()+1)%7`), both fixed-shape in BOTH `get_stats` return paths; `/api/events` gains the optional `file` filter; UI: Overview coupling card (hidden at 0 pairs) + pure-SVG punch card (`punchCard.tsx`, RAMP imported from the calendar — single ramp source), `FileStory.tsx` modal (Changes-group file names + coupling rows → one file's chronological life; static snapshot, UTC day separators, mirrored-constant header effort), `streakOf()` calendar-header streak (UTC, today-zero grace, ≥2 only), session pulse (render-derived `last_event_ts < 5min` breathing dot), CLEAN ✓ celebration per the channel matrix (persistent per-repo toast z-30 always + nonce-keyed ~12-particle chip burst while visible + unchanged hidden-only OS alert; `notifyStatusChange` returns the transition), View Transitions crossfade on nav switches only (`withViewTransition` = feature-detect + reduced-motion guard + flushSync); NO schema change (hook + event version + DB untouched), zero new deps, no new endpoints, no new git calls. v0.1.7.1 (2026-07-21): F58 sweep hotfix — `_poll_loop` gains an unconditional safety-net sweep outside the dirty→clean transition gate (every poll: CLEAN repo + unlinked events → sweep to HEAD, `swept=1`); closes the gitignored-plan-edit gap where an event accrues without any git-status change and no sweep trigger ever fires (ordering-correct: `_detect_commits` links real commits first in the same iteration; idempotent; offline-safe). v0.1.8.0 (2026-07-22): replay & recap — `/api/events` gains optional `since`/`until` (ISO-Z TEXT window, composes with every filter); `/api/stats` gains `wrapped` (last-7-UTC-days story: calendar-projected days, top_task/busiest_hour/top_pair with deterministic tie-breaks, files_touched incl. plan files, window commits; fixed shape both paths via `_empty_wrapped`); UI: `dayLanes.tsx` (24h local lanes from fetched rows' repo_ids, mirrored-constant clusters, day picker, repo-scoped alive-flagged ≤3-page fetch, today-only stats-driven refresh) + the in-card replay engine (rAF virtual time from clamped dt, cursor, 8-chip feed, scrubber, 1x/2x/4x; exits on day/scope change, rAF cancelled on unmount) + `WrappedCard.tsx` ("Your week ✨" modal, init-once snapshot, client-resolved title, exported punchCard DAYS); the planned KPI count-up was DROPPED as already shipped in v0.1.4.0 (IMPL-1); NO schema change, zero new deps, no new endpoints, no new git calls. v0.1.9.0 (2026-07-26): skyline & arcade — `commit_info` format gains `%P` (the ONE `git show` call; zero new git invocations; "" = root, distinct from pre-upgrade NULL), `commits.parents TEXT` via the needed-columns migration loop generalized to a per-table map (NULL persists only beyond the last-20 backfill window, which self-heals on the first restart), `/api/history` rows flow the field free; UI: `skyline.tsx` (pure-SVG 2:1 dimetric city of the 365-day calendar — sqrt heights, back-to-front painter order, shared `rampBucket` lifted from calendarHeatmap, flat-identical tooltips) behind a persisted `flat | city` card toggle (default city) with the legend lifted to one exported `<RampLegend>`; `trophies.tsx` (7 ranked trophies —/C..SSS + 2 secrets, frontend-only, scope-aware); `comboMeter.tsx` + App WS combo (refs mirrored to state, mirrored GAP decay, milestones {5,10,25,50,100,250} with the celebration burst recipe whole incl. the ~900ms nonce-compare clear, live-only); `mermaidGraph.ts` gains `buildGitGraph`/`renderGitGraph` on the ONE initialize (gitGraph themeVariables added; sanitized `mainBranchName`; merges decorate solely with "*"-suffixed side tips + M's id on the merge statement; octopus #3+ list-only; cap 20) and History gains the "⎇ graph" expand card (GraphPanel effect recipe verbatim: alive flag + inline error note + busy; DOMParser+adoptNode; mermaid chunk still first-expand lazy); ONE additive schema column + one additive `/api/history` field, no new endpoints, no new git calls, zero new deps. v0.1.10.0 (2026-07-27): identity & focus — `/api/stats` gains `identity` (top-8 basename extensions + ext_total + sessions + first_event_ts + ALL-TIME commits) and `file_churn` (top-20 by events, with last_ts), BOTH fed by ONE new file-level GROUP BY with the Amendment-A1 DOUBLE plan-file exclusion (`_is_plan_file`: tasks set OR the documented PLAN_*.txt basename — retrofitted into the shipped coupling's both call sites, so task-less plans leave coupling/top_pair/identity/churn while events_per_task + wrapped.files_touched stay deliberately inclusive), fixed shapes both paths; UI: `identityCard.tsx` (stroke-dasharray donut + facts line), `churnMap.tsx` (hand-rolled Bruls squarify, fixed 400x180 viewBox, recency-band fills via the shared RAMP, tiles open FileStory), the new identity|churn 2-col Overview row, and `focusMode.tsx` (z-50 ambient wall: live hero, minute clock, scope-snapshotted "in flight" feed, pulse, effort/streak line, Skyline strip; scope snapshot + focus trap on ✕ + the GraphShell open-effect recipe; palette entry + Legend line; Esc/✕ only); no schema change, no new endpoints, no new git calls, zero new deps. v0.1.11.0 (2026-07-28): rings & constellations — the FIRST all-frontend release (Backend touched only at the version constant): `goalRings.tsx` (Apple 2-circles-per-ring daily goal rings from `calendar[last]`; localStorage `katlab.goals` with per-field fallback + rejection-not-coercion edits; ring-close burst = the celebration recipe whole + unmount timer clear; crossings fire on stats changes only — the rings instance alone is keyed by scope, and goal edits can never false-fire by construction (prev stores raw values, the compare reads the goal fresh — CFT-1); inline ⚙ popover, non-overlay) opening the now-3-col rings|identity|churn row and reused `compact` on the focus wall; `couplingArcs.tsx` (arc-diagram constellation of the shipped coupling pairs — weight-ordered baseline nodes, semicircle arcs on the list badge's exact bucket expression with `couplingMax` as a prop, FileStory-opener nodes, fixed 400x150 viewBox) behind a `list | arcs` coupling-card toggle (default arcs, persisted `katlab.couplingView`, list byte-identical); zero new deps, endpoints, git calls. v0.1.12.0 (2026-07-29): momentum & presence — `momentum.tsx` (week-over-week strip between KpiRow and the charts: calendar `[-7:]` vs `[-14:-7]` sums per metric; honest delta rule — prev==0 && cur>0 → "new ▲", never a pct against zero; ▼ slate not red; 14-day polyline pairs sharing point 6, prior week 0.35 opacity, goal-rings metric palette) and the PWA shell: `Frontend/public/manifest.webmanifest` (standalone; background `#020617` = body slate-950, theme `#0f172a` = header slate-900 — RV3, the fields differ by design) + `Scripts/gen_pwa_icons.py` (stdlib zlib/struct PNG writer, committed 192/512 icons) + `sw.js` (NETWORK-ONLY installability worker — no Cache Storage ever, C6-safe; SW fetch never intercepts ws://) + main.tsx feature-detected registration; `main.py` gains FOUR explicit FileResponse routes inside the FRONTEND_DIST guard (manifest application/manifest+json; sw.js application/javascript + no-cache; both icons image/png) — deliberately NO StaticFiles mount for /icons (RV2: StaticFiles raises at construction on a missing dir; the start script rebuilds only when dist/index.html is absent, so a stale dist would kill startup — explicit routes degrade per-request); App badge effect on `[repos]`: n = non-offline SUM(count) — exactly the ALL-tab uncommitted KPI basis (RV1), `setAppBadge`/`clearAppBadge` feature-detected + .catch-silenced, installed-window-only by API design; zero new deps, no schema/API/git-call change, no new localStorage keys. v0.1.13.0 (2026-07-31): snake, pet & clock — `snakeGame.tsx` (third calendar view `flat | city | snake`: serpentine year-eater from the FIRST non-zero column (col-0 fallback, absolute-column direction parity), eaten derived from head index, head + min(5, k+1) growing segments, rAF engine at min(24, pathLength/4) positions/s — a ~4s minimum show on young installs, ~15s full year; calendar snapshot per run; reduced motion = static + parked, JS guard); `pet.tsx` ("Kat" — pure `moodOf(repos, comboCount, comboLastMs, nowMs)`: excited only while the combo is LIVE per the ComboMeter's exact window > sleeping > content > anxious (48h+) > curious; `UNCOMMITTED_AGE_H` lifted App.tsx → theme.ts, ONE source for bell + pet; five SVG mood faces, pet-* keyframes joining the ONE reduced-motion media block; header chip left of the ComboMeter + big on the wall via FocusMode's new `mood` prop); `dayLanes.tsx` gains the `lanes | clock` toggle (persisted `katlab.dayView`; clock disabled while replay armed; module-private `DayClock` — 24 local-hour wedges from the SAME fetched rows, outer 26 + sqrt(count/dayMax)·84, RAMP-bucket fills, zero-hour ticks, today-only render-computed hand; empty day renders NO dial — the shared paragraph covers it); zero backend (version.py only), zero new deps. v0.2.0.0 (2026-08-02): KATLAB City — the 4th top-level view (`city.tsx`; View union + nav + palette, workspace-wide by design — full repos/tasks/events props, never tab-filtered): one iso district per repo (module-private 2:1 dimetric at city scale), buildings from each repo's OWN scoped top-20 file_churn via PER-REPO `api.stats(repo.id)` calls (the global top-20 starves quiet districts — proven live 13/7/0) THROTTLED to one round per 10s with one trailing catch-up (get_stats is the heavy endpoint; the tab-scoped App stats prop = freshness nonce only); heights 14 + sqrt(events/cross-district churnMax)·64, treemap recency tints, StatusBar-mirror plaques, in-progress cranes (cap 3+N), FileStory/onGoRepo click-throughs; LIVE: id-keyed event rain (per-batch prune timers), count-drop fireworks (values-ref diff, per-district nonces), half-open local-hour sky buckets + 18 deterministic stars, walking Kat (the App mood); city-drop/kat-walk/burst-p under the one reduced-motion block; zero backend (version.py only), zero new deps, no new localStorage keys — the v0.2 line marks the milestone, the cross-repo contract is unchanged. v0.2.0.1 (2026-08-02): report & badge — `reportHtml.ts` (the self-contained HTML report: string builder on the digest recipe; 7d/30d calendar-tail ranges; range-true hero — commits from the calendar sum, never all-time; all-time sections explicitly "(all-time)"-marked; wrapped 7d-only; no version claim in the footer; Overview "Report ⬇" + two Actions palette entries, stats-null no-ops) and `Backend/app/badge.py` + the main.py root route `/badge/{repo_id}.svg` (380x80 dark SVG card: teal ring mark, XML-escaped repo id, `{:,}` 7d COUNT via thread-local get_conn — the substr day-bucket law, db.py untouched — plus the StatusBar-palette chip; 404 unknown; image/svg+xml + max-age=300; LOCAL previews only — github.com camo cannot reach 127.0.0.1); the guideline + Onboarding_Prompt gain the OPTIONAL badge-embed section (first contract addition since v0.1.10.0, optional); backend diff = version.py + main.py + badge.py; no schema/endpoint/git-call change, zero new deps. v0.2.1.0 (2026-08-02): records & snapshots — `records.tsx` (four calendar records: best day captures/effort, longest streak via the exported maxStreakOf — the shipped streakOf is CURRENT-run-only, proven divergent live 11 vs 9 — and bestRolling7; display spans the full calendar, detection excludes today with the strictly-greater law via prev-raw-values — the goalRings crossing recipe; scope-keyed at the call site; the new lg:grid-cols-2 ARCADE SHELF wraps TrophyCase | Records — a deliberate placement change); `favicon.ts` (canvas → data-URL swap of the rel="icon" link; the app-badge non-offline basis VERBATIM ×2 — presence surfaces never disagree; clampCount >99 → "99+"; static favicon.svg stays; Safari ignores dynamic favicons); the City snapshot (flex h2 + button; the exported inlineSvgClasses inlines the scene's four fill classes to hexes + strips class attrs — standalone SVGs have no Tailwind; overlays excluded by construction; KATLAB_City_<date>.svg); all-frontend (version.py only), zero new deps. v0.2.2.0 (2026-08-03): the plan board — `planBoard.tsx` (`groupActivePlans` exported: tab-scoped tasks grouped by (repo, plan_file), ACTIVE = ≥1 non-done, done-only never renders, zero actives hides; sort max(last_event_ts) DESC nulls-last deterministic; segmented done/in-progress(pulse-dot)/pending bar in served order; the in-progress SPOTLIGHT with SHORT ids via split(" - ").pop() — full refs in title attrs — 2-line-clamped why + 4-cap file chips → FileStory; next-up = first pending; "no task in progress" honest state); placed as a SIBLING of the KpiRow line BEFORE both totalEvents branches (task-driven, never event-gated); hygiene: postcss lockfile bump 8.5.16 → 8.5.25 (+ nanoid in its tree; the Dependabot-HIGH patch, package.json untouched); all-frontend + lockfile (version.py the only Backend touch), zero new deps. v0.2.3.0 (2026-08-04): editor & health — the FileStory header gains the ONE `editor ↗` anchor (repoPath prop via the repoBranch precedent; `vscode://file/` + encodeURI(backslash-normalized absPath) + post-encode `#`→`%23` — `#` is legal on Windows and encodeURI leaves it raw; null-guard; VS Code user-confirmed); `GET /api/health` in routes.py — the first REST addition since /badge (v0.2.0.1), READ-ONLY (zero git calls, zero DB writes): server block (version, STARTED_TS import stamp, guarded DB_PATH.stat, watchers alive/total via tracker._tasks done() scan, hook line-presence TEXT scan of ~/.claude/settings.json + the checked path) + per-repo block in config order (db.get_last_event_ts — the D9 heartbeat read, guarded repo.events_file stat with ISO-Z mtime matching last_event_ts, warning counts via the /repos .get precedent); `healthPanel.tsx` — neutral "sys" header chip (no polling) + fetch-at-open modal (STABLE onClose) with EXPORTED presentational HealthBody({data}); honest wordings: hook "line present ✓/line missing ✗" (presence, not registration), "—" on modeled nulls, the jsonl-size title attr = last-log-write (the ingest-lag hover; suppressed when null); palette entry under Views; backend diff = version.py + routes.py, zero new deps. v0.2.4.0 (2026-08-06): the Chronicle — an ISOLATED MkDocs 1.6.1 module (`Scripts/Chronicle/`: stdlib generate.py + pages.py + five bats + own requirements.txt, plain pip; product runtime deps untouched, backend diff = version.py only) generating a living docs site into gitignored `Chronicle/runtime/` from the RUNNING server's REST only (envelope unwrap; pages of 500 cap 20; ~10s timeouts; UTF-8 both ends; ALL fetches before the first write): Overview (the ONLY data-through stamp), per-UTC-day Devlog (served calendar minutes, "(unattributed)" bucket, stale-ref law "(plan no longer present)"), Plans (path-flattened filenames + full-path nav labels — the bare-basename collision law), Changelog-by-reason, byte-verbatim Docs mirror (incl. LICENSE for --strict link validity) + temp/Ref mermaid diagrams page; write-only-if-changed ATOMIC sink (tmp+os.replace) + FULL-expected-set stale-page sweep + calendar-diff incremental loop ticks (catch-up-ingest safe); md + YAML escape surfaces; serve on 8200 via `python -m mkdocs serve --livereload` (CFT-5: no --dirty — our 0.7s full builds never needed it and full rebuilds regenerate the nav live; the Live_arch click>=8.2 + UMD-mermaid + use_directory_urls:false + fence_mermaid + fix_windows_paths-hook tricks harvested verbatim — the hook is GENERATED into the runtime, IMPL-1); live.bat loop = own titled window + heartbeat lockfile at the runtime root + self-exit on dark serve port; UI untouched (palette entry backlogged R-AY). v0.2.5.0 (2026-08-11): the Scribe — `Scripts/Chronicle/scribe.py` (claude -p HEADLESS on subscription login, never --bare; stdin JSON data doc + instruction prompt; the 5-condition gate incl. the "](" link reject; communicate-retry spawn with per-retry lock heartbeat — drain-safe, CFT-2-safe; once-ever EXISTENCE idempotence + the fail-CLOSED 5-runs/UTC-day cap counter `.scribe_runs.json` at the runtime root, assemble→reserve→invoke no-refunds, 7d horizon, deterministic drain; RV44 two-basis minutes — per-repo scoped never summed, totals unscoped) writing AI-authored diary/weekly/release-draft pages with the authorship footer into runtime `docs/story/`; generate.py: run_once stamps `_fetched_at` PRE-fetch + returns the model (the midnight-race gate), loop LAZILY imports scribe (one-shot never scribes) + passes its lock-utime callable; nav "Story" section from the RECOGNIZED-pattern scan (index.md excluded, alien files ignored, hidden at zero, index generate-owned emitted-or-removed) via `pages.build_story_index`; `scribe.bat` = manual drain; backend diff = version.py only. v0.2.6.0 (2026-08-12, "One System"): the Chronicle becomes a true SUB-SYSTEM — main.py serves the BUILT site at `/chronicle/` (per-request GET+HEAD resolver: FastAPI never auto-adds HEAD (405-proven) and both self-healing mechanisms are HEAD requests; traversal-guarded 404-never-500; uniform no-cache; the meta-refresh self-healing not-built page; mkdocs' absolute-path 404.html never served) and its lifespan SPAWNS the regen loop (system-python spawn/probe — the venv has no mkdocs, live-proven; demo never spawns; CREATE_NO_WINDOW → data/logs/chronicle.log self-made dir; terminate on shutdown, tree-kill + tracker-port orphan watch as belts); generate.py: --build (non-strict `sys.executable -m mkdocs build` into site.new + the RENAME-FIRST atomic swap — the old site serves whole through any failure — with rollback + orphan-dir self-clean), the lock's crash-window retry (~30s×15min), the dark-port watch retargeted 8200→the tracker port, site_url = config BASE + /chronicle/; pages.py emits refresh.js (15s visible-tab Last-Modified HEAD poll; file:// and null guards) wired into extra_javascript; the UI gains the 5th view "Chronicle" (same-origin iframe + 10s re-probing not-built card + header 📖 chip + two palette entries + the focus-boundary Legend note); R-BD silent ops: pythonw via the `start "" /b cmd /c` wrapper incantation → data/logs/tracker.log (rotation, RV8 already-running guard, RV27 mkdir, config-read browser open), demo silenced (demo.log), uvicorn access_log=False (pure-signal logs); serve.bat + live.bat + _start_all.bat DELETED (the mkdocs-serve all-404 class extinct); backend diff = main.py + git_module.py (the R-BD no-window flag) + version.py. Post-impl CFT loop (18 passes, CFT-1..14 fixed in place, 5/5 clean close — see the plan CFT LOG): the bare-dir 307, the -u loop log, view.bat -> site.view, the RV16 lock TOCTOU guard, the guarded pre-yield spawn, serve/live residue + doc truth. v0.2.7.0 (2026-08-15): Alive & Rewarded — `weatherOf` + per-district WEATHER in the pure CityScene svg (fog=offline / sun=CLEAN with the night-bucket MOON / rain=aged-uncommitted on the ONE-source UNCOMMITTED_AGE_H chain / fresh=null; scene-not-overlay so snapshots stay honest, attrs-only fills, index-math geometry, wx-* motion as animation:none under reduced motion, pointer-events off); `wardrobeOf` + `WARDROBE_TIERS` in pet.tsx (EARNED accessories from the served UNSCOPED calendar via records.tsx's maxStreakOf/bestRolling7 — the pet mirror law whole: zero persistence/localStorage; collar 7d/bandana 21d/crown 50d streak, scarf 500-week, star 300-day; the RV20-deterministic next-tier tip) fed by ONE 60s-throttled unscoped api.stats() in App threaded to all three Kat sites (header, focus wall, City street via the HTML overlay — the scene never sees it); the RELEASE moment (theme.ts RELEASE_RX mirror of scribe.py's message-END law + prefix3 — the RELEASE-LINE LAW: fire on 3-part-prefix change only, every KATLAB commit is version-ENDed; baseline seeded once-latched on the first non-empty repos payload with only-if-null writes; z-40 per-repo banner STACK top-level on every view, cap 3, 12s nonce dismissals, 24p hero burst, changelog deep link, notify.ts trigger (4) notifyRelease hidden-only); backend diff = version.py only; battery 45/45 on real sources + live parity 14-streak/679-week/312-day (collar+scarf+star ON at ship). Post-impl CFT loop (8 passes, CFT-1..3 fixed in place, 5/5 clean close — see the plan CFT LOG): the un-clipped hero burst, the JSX-comment syntax fix + the pipe-masked-build law (builder's own exit + served-artifact grep), the notes' file-count truth; battery re-executed 35/35 on the final tree. v0.2.8.0 (2026-08-16): Sound & Sky — `sound.ts` (the notify.ts audio twin; all-synthesized opt-in cues: the capture tick pitched by the HIGH-BITS Knuth hash (Math.imul(id,2654435761)>>>27)%5 onto the pinned A4/B4/C#5/E5/F#5 quintet with an 80ms drop-limiter, the E5→A5 CLEAN chime, the A4→C#5→E5 release fanfare — the ONE-KEY consonance law makes every same-moment overlap consonant by construction; katlab.sound opt-in default-off, visibility-independent — the channel matrix's 4th column; gesture-law AudioContext + first-interaction reload resume, feature-detected inert-off; the RV15 enable order key→resume→confirmation; exported NOTES/TICK_STEPS/tickStep/tickAllowed for the battery) wired at three App sites (capture tick, CLEAN chime, release fanfare) + the OS-alerts-twin toggle (bell footer + palette) + a Legend line; `weatherOf` gains the 5th state "forecast" (the symbolic (rain−12h, rain] window — WX_FORECAST_LEAD_H beside the fn, both bounds self-adjusting; one still lighter cloud, attrs-only, snapshot-riding, reduced-motion-immune by construction; the computed "rain in ~Nh — commit to clear" tip with the max(1,ceil) zero-hour guard; WEATHER_TIP narrowed to exclude forecast, the title builder three-way; scene-only — never a ping); backend diff = version.py only; battery 31/31 incl. the low-bits-ladder DISCRIMINATOR row; served bundle artifact-grepped. Post-impl CFT loop: 5 passes, ZERO product findings (the third zero-finding CFT — the review's 17 pre-impl fixes left nothing) — battery re-executed 21/21 on the final tree, dist mtime-proven, live sweeps clean. v0.2.9.0 (2026-08-16): Drafts & Daydreams — `draft.ts` (the THIRD moat inversion vs the aicommits class: buildCommitDraft composes the subject from KNOWN attribution — ascending-id mission-arc order over the DESC-served pool, the compound (repo,ref) lookup, stale refs bare, "+N unattributed" honesty, the probed `KATLAB <TAG>: vX.Y.Z.W - <summary> - vX.Y.Z.W` frame with placeholder slots END-anchored-RELEASE_RX-immune; copyCommitDraft feature-detected clipboard-only-forever) surfaced as the StatusBar dirty-repo `draft 📋` chip (the as-built per-repo home) + per-dirty-repo palette actions + the repo-keyed note; the recovery RAINBOW in CityView (rain→sun only, the fireworks recipe, pinned arcs 60/54/48/42 outside-in red-first on the y=170 rooftop baseline, overlay-transient — snapshot-excluded, display:none reduced-motion; stacks with fireworks as the documented recovery sequence); ATTRACT MODE in App (10-min idle → 25s City/Overview/Chronicle carousel under a transparent CLICK-CATCHER — exit on CLICK never pointerdown (the unmount click-through law), keys wake with intent-honoring chord pass-through; suppressed by overlayOpen/healthOpen/hidden/chronicle-view — the iframe blind spot never arms; katlab.attract opt-out default-ON, the 7th key; the remount-fetch cost documented-accepted); backend diff = version.py only; battery 15/15 incl. the repo-scope-leak and DESC-order rows; served bundle artifact-grepped. Post-impl CFT loop (6 passes, CFT-1..2 fixed, 5/5 clean close): the StatusBar seam wording aligned + the draft note's nonce-compare clear (the celebration law). v0.2.10.0 (2026-08-16): Flow & Milestones — `flowState` in theme.ts (the effort law's LIVE face: on = count≥2 && gap alive on the ONE-source EFFORT_GAP_MAX_MIN; minutes = max(1,floor) — the wxTip clamp, clock-set-back-safe) + `flowChip.tsx` (PURE per the pulse recipe — zero hooks, derives at render, the P8 60s tick grows/expires it; seated immediately RIGHT of ComboMeter — the Pet adjacency law keeps the left seat; sky/water vs amber/fire; fmtMinutes text — the one-humanizer law) fed by the new comboStartMsRef (stamped at next===1 beside the combo refs); THE FLOW-SOURCE LAW: flow rides the LIVE chain refs, never the uncommitted pool — a commit never breaks flow (the pool loses events on every sweep; the refs are written exclusively in the event_resolved branch); session-live honesty (reload/reconnect restarts at arrival time, under-reporting only). ODOMETER_MILESTONES in theme.ts (event IDS EVER {5k..1M} on the one global AUTOINCREMENT line — ids never reused; DISTINCT from App-local COMBO_MILESTONES chain COUNTS) checked at the event_resolved id-read site; the moment = the amber card FIRST in the celebration toast column (bottom-anchored growth — the 8s transient never shifts the persistent CLEAN records; no ✕; outside-click-immune; nonce-compare timer) + the self-gated chime (no new cue — the sound once stays spent); UI-closed misses accepted (the WS-gap law), brief gaps self-recover via the F29 replay. Backend diff = version.py only; zero new deps/keys/sounds; battery 7/7 + artifact-proven; CFT 5/5 ZERO findings (the fourth zero-finding CFT); commit cd11779. v0.2.11.0 (2026-08-18): The Provenance Ledger — one new read-only aggregate in `get_stats` (`provenance`: commits_observed / commits_pre / slots_total / slots_ai / top_files[8]) feeding a pure `provenance.tsx` card seated FULL-WIDTH below the Overview arcade shelf; backend diff = db.py + version.py, zero deps, zero keys, zero WS/API surface. THE OBSERVATION GATE: a commit is measured only from its OWN repo's FIRST capture onward (per-repo MIN(events.ts), never the scope-wide min) — the startup backfill seeds ~20 pre-tracking commits per repo (watcher._backfill_commits) which would otherwise read '0% AI' as if measured; excluded commits are COUNTED and surfaced ('N earlier excluded'). MIXED-PRECISION BOUNDARY: events.ts carries microseconds, commits.ts is whole-second, so the ISO-Z text compare admits a commit sharing the exact second of the first capture; accepted (at most one commit per repo, and it contributes honest human slots — it DILUTES, never inflates). THE INTERSECT LAW: a file-slot is AI-touched only where the commit's files_json meets THAT commit's own linked events — the F9/F32 sweep links leftovers to HEAD, so linked-event files outside their commit's file list exist at scale and must never mint slots. THE COMPOSITE-KEY LAW: the event map is keyed by the PAIR (repo_id, commit_hash), never the hash alone — that pair is the commits PRIMARY KEY, and two repos really can share a hash via common upstream history — the DB holds 16 such hashes, ALL of them EA_Dev<->EA_Exec. MEASURED PRECISION (the guard is not fixing a live wrong number): EA_Exec is not in Config/repos.yaml, and among the CONFIGURED repos zero hashes are shared, so no served scope currently holds both sides of a collision. The pair key is kept because the DB proves the topology occurs and the house Release-mirror convention makes it normal — a hash-only map would leak one repo's events into the other's commit and inflate its share the moment two such repos are configured together. SLOT BASIS: volume-weighted per (commit, file) — a file in k commits counts k times — NOT lines of code; plan files excluded on both sides via the A1 double exclusion (tasks-table membership OR the PLAN_*.txt basename). MEASURED SCOPE OF THAT EXCLUSION (do not overstate it): across the CONFIGURED repos it currently filters ZERO files, because EA_Dev and UM_Dev gitignore the temp/ trees their plans live in, so no plan file ever reaches files_json there; the known counter-example (Traceability/Plan/PLAN_Traceability_Phase2.txt, 2 occurrences) sits in EA_Exec, which is NOT in Config/repos.yaml and therefore never reaches a served payload. The guard is kept because a repo that commits its plans is a supported shape and would otherwise inflate its own AI share with meta-edits — but as shipped it is a guard, not an active filter. THE ONE OVER-CLAIM PATH, documented rather than engineered around: swept events attach to HEAD, so if an edit is reverted/never committed, nothing links it, and a later HUMAN commit touches that same file while it is HEAD, the file reads AI-touched in a commit Claude did not shape — four conditions, self-healing on the next commit; the metric is 'conservative in the common case with one rare sweep-induced over-count', NOT 'never over-claims'. HONESTY IN THE UI: pctOf reserves 100/0 for the exact cases (396/397 -> 99%, 1/400 -> 1%) while the bar keeps the unclamped ratio; the tooltip states the basis; 'code written by AI' is forbidden by test. DEMO MODE READS 0% BY DESIGN: Demo/runtime/demo_repo sits INSIDE this working tree, so git resolves upward and the demo ingests TrackingMonitor's OWN commits, while its events are synthetic paths — the two sets cannot intersect. A pre-existing quirk this release merely makes visible; user-ruled to ship as-is, recorded here so a future maintainer never 'fixes' a demo that is not broken. The 📖 header chip retired (it duplicated the Chronicle tab exactly; tab, both palette entries and ChronicleView untouched).

## 3. Multi-Repo Design

- The server tracks **N repos simultaneously** — each with its own `events.jsonl`, plan files, and `.git`
- Repo registry: see [Monitored_Repos.md](Monitored_Repos.md)
- Divide-and-conquer: hooks + guideline are authored HERE; each provider is registered
  once at user scope by the operator; each monitored repo self-installs only gitignore
  + plan rules per the **Installation Guideline**; `Config/repos.yaml` is the allowlist.

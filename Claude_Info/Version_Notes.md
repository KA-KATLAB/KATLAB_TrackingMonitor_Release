# Version Notes

## v0.2.12.0 — Clarity at Every Size (2026-09-06)

**Key Highlights**

- Established [UI_Design_System.md](../Docs/UI_Design_System.md) as the normative interface contract, backed by semantic tokens, shared controls/surfaces, structural icons, bounded paging, and one reactive reduced-motion source.
- Rebuilt the application shell for eight verified phone, landscape-phone, tablet, and desktop sizes. Tasks is a modal drawer below 1024px and a persistent sidebar from 1024px; all five views, repo scopes, status, Attention, and utilities remain reachable.
- Made `{scope, view}` canonical navigation state with deterministic query ordering and Back/Forward restoration. Sensitive paths, filters, drafts, results, and Blob data never enter the URL or browser history state.
- Consolidated every modal and drawer onto `DialogShell`: portal rendering, one overlay lease, inert/scroll-frozen background, dynamic Tab containment, Escape/backdrop policy, and safe focus restoration including More-to-dialog handoff.
- Reorganized Overview into Now → Trends → Explore → Relationships; replaced the attribution doughnut with an exact horizontal bar; added semantic, keyboard-accessible exact-data alternatives without removing the established charts, City, Chronicle, rewards, or product personality.
- Bounded large rendered collections to 50 rows and City to six districts while calculations, filtering, actions, graph inputs, and exports continue to use the complete model. History fetch depth remains independent of its visible page.
- Added one 10-second foreground-action deadline, abort/stale-generation protection, exclusive heavy-work coordinators, truthful retry states, a two-stage prepared digest, and safe delayed Blob URL cleanup for all generated downloads.
- Production verification passed: 40/40 view/viewport geometry, 16/16 interaction, 10/10 semantics, 37/37 action inventory, 4/4 dialogs, 8/8 navigation, 3/3 reduced motion, 12/12 performance, 7/7 generated-document checks, 4/4 downloads, 3/3 forms, 2/2 City target checks, and 27/27 focused source/unit checks. Measured CLS stayed below 0.014.
- Initial JavaScript fell from 519,299 raw / 169,031 gzip bytes to 339,958 / 108,076; Overview, City, and Mermaid remain lazy. No product dependency, schema, endpoint, Git behavior, storage-key, service-worker, or Chronicle-runtime change was introduced.
- Full as-built evidence and scope are recorded in [TrackingMonitor_v0.2.12.0_Release_Notes.md](../TrackingMonitor_v0.2.12.0_Release_Notes.md). The local release record is complete; the live tracker was not restarted and publication remains a separate user action.

## v0.2.11.0 — The Provenance Ledger (2026-08-18)

**Key Highlights**

- Twenty-fourth harvest cycle — user ruled "follow your recommendation" (2026-08-17): R-BP The Provenance Ledger ALONE (the 2026 "AI code share" class inverted — exceeds.ai/Co-Authored-By/detector approaches all GUESS the attribution at 60-70% accuracy; the tracker OWNS it per file, with the reason, linked to the commit). Moat inversion #4 after the Chronicle, the Scribe and the Commit Draft. Follow-up ruling (2026-08-18): the demo's structural 0% ships AS-IS
- **Provenance card** (`Frontend/src/provenance.tsx`, Overview, full width below the arcade shelf): the share of committed FILE CHANGES that carry captured Claude events — 73% at ship (288 of 397 slots over 29 observed commits), one large percentage over a two-tone bar, then the top-8 files with their per-file ratios (each row opens the existing File Story). Never a lines-of-code claim: the tooltip states the basis, and rows like the compiled `.ex5` artifacts honestly read 0/14
- **The observation gate** (`db.py` D2): a commit counts only from its OWN repo's first capture onward. The startup backfill seeds ~20 pre-tracking commits per repo, which would otherwise read "0% AI" as if measured; they are excluded AND surfaced ("40 earlier excluded")
- **The intersect + composite-key laws** (D3): a file-slot is AI-touched only where the commit's file list meets THAT commit's own linked events, keyed by the PAIR `(repo_id, hash)` — the commits primary key. Two repos can share a hash via common upstream history — 16 such hashes in the DB, ALL EA_Dev<->EA_Exec, and since EA_Exec is de-configured ZERO are shared among the configured repos, so the pair key is a guard against a proven-possible topology rather than a fix for a live wrong number; a hash-only map would inflate one repo's share with the other's events the moment two such repos are tracked together
- **Honesty rules**: `pctOf` reserves 100/0 for the exact cases (396/397 → 99%, 1/400 → 1%); the bar keeps the unclamped ratio; the tooltip states the basis; "code written by AI" is forbidden from the card by test. The ONE documented distortion — a CLEAN-sweep edge that can over-attribute a reverted-then-human-committed file — is written down rather than hidden
- **📖 chip retired**: it duplicated the Chronicle tab exactly. Tab, both palette entries and the Chronicle itself untouched
- **Demo reads 0% BY DESIGN**: the demo's commits are TrackingMonitor's own git history (its scratch repo sits inside this working tree, so git resolves upward) while its events are synthetic paths — the sets cannot intersect. User-ruled to ship as-is; documented so it is never mistaken for a defect
- **Engineering discipline**: 64-pass CDD loop, 82 findings fixed in place, 5/5 consecutive clean. Executed not just read — the aggregate ran live and against a fixture battery that defeats both naive implementations; the card was built from the plan text, rendered (22/22) and type-checked strict. Machine verification: Python battery 9/9, render battery 22/22, `npm run build` exit 0 (which is `tsc -b && vite build`), artifact greps confirmed the three new tokens present and the chip's title string gone while the legend phrase survives, containment exact (6 code files), both servers restarted onto 0.2.11.0
- **Post-implementation CFT loop** (2026-08-18): 34 fresh-eyes passes, 14 findings fixed in place, closed 5/5 consecutive clean. THIRTEEN of the 14 landed in DOCUMENTATION, one in a code comment — the shipped logic was clean from the second pass and never changed again. The heaviest were honesty regressions in what we SAID: two shipped claims still repeated premises the plan review had retired (the A1 exclusion called "load-bearing" when it filters zero files among configured repos; the composite-key law credited to "two MONITORED repos" when all 16 shared hashes involve de-configured EA_Exec), and the legend told users "0/N simply means Claude never touched it" — an absolute later MEASURED false for 5 of 62 zero-files, which do have captures linked to other commits. That correction then got its own guard (new battery row V18). Clean passes proved rather than asserted: Architecture_Notes is complete enough to REBUILD the aggregate byte-identically, the card satisfies all 15 release-notes claims, the gate partitions the commit set exactly (observed + pre == identity's independent all-time COUNT in all scopes), the per-file rows decompose the headline exactly with an honest top-8 truncation, and the AI-touched slots are real Edit captures carrying real plan-task reasons
- **Scope:** backend diff = `db.py` (one read-only aggregate) + `version.py`; no schema/endpoint/hook/git-call change; zero new pip/npm deps (`package.json` byte-identical); ZERO new localStorage keys (still 7); no new WebSocket message type. The card rides the existing `/api/stats` payload and refetch, so a commit updates it in ~300 ms. Six code files touched: db.py, version.py, charts.ts, provenance.tsx (new), OverviewView.tsx, App.tsx

## v0.2.10.0 — Flow & Milestones (2026-08-16)

**Key Highlights**

- Twenty-third harvest cycle — user ruled "go with your recommendation": R-BN Flow Chip + R-BO Odometer Moments as one light release (the fatigue-literature bar: NEW INFORMATION, not more decoration; Monthly Wrapped stays parked ~Oct). Plan passed a 21-pass 4-step CDD review loop (13 findings RV1-RV16 fixed in place, 5/5 consecutive clean RV17-RV21) — heaviest: RV1 the odoNote placed above a slot that does not exist (draftNote is StatusBar-inline), RV2 the chip's self-tick violating the pulse recipe (the P8 60s tick already expires chips) + the "flow"-vs-"overflow" bundle-grep collision, RV4 the seat conflict with the shipped Pet adjacency law (reseated RIGHT of ComboMeter), RV12 the raw `{minutes}m` bypassing the ONE effort humanizer (fmtMinutes carries the ≈ — "93m" would have shipped where the house says "≈ 1h 33m")
- **Flow Chip** (A.1-A.3/R-BN — the 2026 flow-tracker class inverted: DepthWork/Rize/Flowtime GUESS flow from app focus; the tracker OWNS capture ground truth): `flowState(count, startMs, lastMs, nowMs)` in theme.ts — the effort law's LIVE face (on = 2+ chained captures with the gap alive; minutes = max(1, floor) — the wxTip clamp, which also gracefully covers a clock-set-back negative); `flowChip.tsx` PURE per the pulse recipe (zero hooks, zero state — derives at render with Date.now(); the P8 tick grows and expires it), seated immediately RIGHT of ComboMeter (sky/water 🌊 vs the combo's amber/fire 🔥 — structurally identical siblings), text through fmtMinutes ("🌊 flow ≈ 5m" / "≈ 1h 12m"), tooltip interpolating EFFORT_GAP_MAX_MIN; THE FLAGSHIP PROPERTY: flow rides the LIVE combo chain refs (comboStartMsRef stamped at next===1), NEVER the uncommitted pool — a COMMIT NEVER BREAKS FLOW (a pool-based flow would falsely die on every sweep); session-live honesty (the combo precedent — a reload restarts the chain; a reconnect burst restarts at arrival time, under-reporting only)
- **Odometer Moments** (A.1/A.3/R-BO): ODOMETER_MILESTONES in theme.ts ({5k, 10k, 25k, 50k, 100k, 250k, 500k, 1M} — event IDS EVER on the ONE global AUTOINCREMENT line, ids never reused even after deletes; DISTINCT from App-local COMBO_MILESTONES chain COUNTS — the two same-suffixed Sets pinned apart); checked at the existing event_resolved id-read site (O(1), all modes, unreachable from other message types); the moment = an amber-bordered card FIRST in the celebration toast column (the bottom-anchored column grows upward — its 8s life never shifts the persistent CLEAN records; no ✕ — a self-expiring moment, not a record; immune to the outside-click toast clear) + the existing chime (self-gated by sound.ts; NO new cue — the sound once stays spent; in-family consonant) + the nonce-compare timer law; copy "🎉 capture #5,000 — the odometer rolls" (en-US locale); UI-closed misses accepted (the WS-gap law), brief-gap misses self-recover via the F29 catch-up replay; live ground truth at ship: max(id) = count = 2,707
- **Engineering discipline**: battery 7/7 on the real sources (esbuild cjs: the count-2 on-boundary, the 150s floor, gap-death, the sub-minute clamp, the milestone set, the scene render with the humanizer's ≈, the locale comma) + V8 both-gates (build's OWN exit 0; the served bundle artifact-grepped for "the odometer rolls" ×1, 🌊 ×2, "unbroken work chain" ×2 — the emoji-escape concern refuted by the bundle's literal 🔥/🎉/≈); evidence-driven review: the live DB fact-check (max id 2,707 — the WHY's pacing claim grounded), the sw.js stale-bundle probe refuted by source (network-only passthrough), the EA/UM methodology copies diffed (one law, repo-local examples)
- **Post-implementation CFT loop** (2026-08-16): 5 fresh passes, **ZERO findings** — the fourth zero-finding CFT in house history (after v0.1.12.0, v0.2.2.0, v0.2.8.0): the 21-pass review left nothing for the code to get wrong; the flagship commit-never-breaks-flow property re-verified in the final as-built read (chain refs written exclusively in the event_resolved branch). COMMITTED: cd11779
- **Scope:** backend diff = version.py ONLY; no schema/endpoint/hook/git-call change; zero new pip/npm deps; ZERO new localStorage keys (7 total unchanged); no new sounds; cross-repo contract unchanged

## v0.2.9.0 — Drafts & Daydreams (2026-08-16)

**Key Highlights**

- Twenty-second harvest cycle — user ruled "all 3 points": R-BL Commit Draft + R-BK Rainbow After Rain + R-BM Attract Mode as one release. Plan passed a 19-pass 4-step CDD review loop (12 findings RV1-RV12 fixed in place, 5/5 consecutive clean) — heaviest: RV4 the Chronicle-iframe blind spot (input inside the iframe never reaches the parent — attract would arm mid-read and a parked carousel could not hear the exit: the never-arm-on-chronicle law + the kiosk CLICK-CATCHER), RV6 the catcher's unmount click-through (exit on CLICK, never pointerdown), RV5 the DESC-served pool vs the mission-arc subject order
- **Commit Draft** (A.1-A.2/R-BL — the THIRD moat inversion: the Chronicle beat git-cliff, the Scribe beat the devlog tools, this beats the thriving aicommits/OpenCommit class, which all LLM-guess from diffs): `draft.ts` composes the commit message from KNOWN attribution — the repo's uncommitted events in ascending-id first-touch chronology (the mission-arc order), joined task titles via the compound (repo, ref) key, stale refs bare, the "+N unattributed" honesty suffix, framed in the probed house shape `KATLAB <TAG>: vX.Y.Z.W - <summary> - vX.Y.Z.W` with BOTH version slots as the literal placeholder (END-anchored-RELEASE_RX-immune — live-proven even when task titles carry real version strings). Deterministic, instant, zero-AI, zero-API, zero new fetches; **clipboard only, forever** (the tracker never runs git). Surfaces: the `draft 📋` chip on every dirty repo's StatusBar entry (the as-built home — the plan's "sidebar card" corrected to the true per-repo surface) + one palette action per dirty repo + the repo-keyed copied✓/blocked✗ inline note
- **Rainbow After Rain** (B.1/R-BK — the recovery celebration): a rain→sun transition (aged work finally committed) arcs a four-band rainbow over the district — the fireworks recipe verbatim (values-ref diff, first-payload baseline, nonce-keyed, ~2.5s), pinned geometry (district-center x, rooftop baseline, radii 60/54/48/42 outside-in red-first — the physical order), overlay-class transient (snapshot-excluded, display:none under reduced motion); forecast→sun and null→sun stay silent — only the full rain state earns it; stacks with the count-drop fireworks as the documented recovery sequence (0.9s fireworks finishing as the 2.5s rainbow blooms)
- **Attract Mode** (C.1/R-BM — the kiosk class, living): after 10 idle minutes the tracker daydreams — cycling City → Overview → Chronicle every 25s via the shipped crossfade under a transparent CLICK-CATCHER (any click or key wakes it and restores exactly the {tab, view} you left; the catcher also blocks accidental interactions — a daydreaming kiosk wakes on touch, never acts on it); suppressed while any overlay/health modal is open, while the tab is hidden, and while the Chronicle is the active view (the iframe blind spot — unmeasurable idleness never arms); opt-out via `katlab.attract` (default ON — the one new localStorage key, 7 total) + a palette entry + a Legend line; documented-accepted: the carousel's re-entries re-run view mount fetches (~2 heavy calls/75s — an idle machine's budget)
- **Engineering discipline**: battery 15/15 on the real sources (the mission-arc order over a DESC-shaped pool, the repo-scope leak row, dedup, immunity, both placeholders, the tidy all-unattributed frame, the verbatim-TAG fallback — live-exercised by the demo's own "Demo" id, copy-in-node resolves false, the rainbow transition table, the pinned arcs); a live dry-run composed a sane subject from 52 real EA events pre-implementation; the served bundle artifact-grepped (draft frame, rainbow class, attract key); tsc + vite green (real npm exit)
- **Post-implementation CFT loop** (2026-08-16): 6 fresh passes, 2 findings fixed in place — CFT-1 the plan's "sidebar card" wording aligned to the as-built StatusBar home (the seam correction stamped at implementation, audited on schedule); CFT-2 the draft note's repo-keyed clear violated the nonce-compare law (a rapid re-click's note died early — nonce'd, rebuilt, artifact-proven); 5/5 consecutive clean close with the battery re-executed 15/15
- **Scope:** backend diff = version.py ONLY; no schema/endpoint/hook/git-call change; zero new pip/npm deps; ONE new localStorage key; cross-repo contract unchanged

## v0.2.8.0 — Sound & Sky (2026-08-16)

**Key Highlights**

- Twenty-first harvest cycle — user agreed with the recommendation: R-BH The Sound of Work (the backlog's registered sound may-propose-ONCE — taken, the once is SPENT) + R-BJ Weather Forecast as one release; R-BI time-lapse not taken (R-AQ stays parked), **R-AJ CLI card RETIRED permanently**. Plan passed a 21-pass 4-step CDD review loop (17 findings RV1-RV17 fixed in place, 5/5 consecutive clean) — heaviest: RV10 the reviewer's own hash fix convicted by LIVE data (the low-bits Knuth mod-5 is still a ladder since K ≡ 1 ≡ 2³² mod 5; the shipped high-bits form numerically verified uniform), RV3 the WEATHER_TIP Record type demanding a fourth key, RV15 the confirmation tick swallowed by its own guard if played before the key write
- **The Sound of Work** (A.1-A.2/R-BH — the tracker's FIRST audio channel): `sound.ts`, the notify.ts audio twin — ALL synthesized (zero files, zero deps): a ~50ms rain-drop tick per live capture (pitch = the high-bits Knuth hash `(Math.imul(id, 2654435761) >>> 27) % 5` onto the pinned A-pentatonic quintet A4/B4/C#5/E5/F#5 — deterministic, never a do-re-mi ladder; an 80ms DROP-limiter makes a 20-file Claude turn patter and a 50-event catch-up ~2 ticks), a warm CLEAN chime (E5→A5), a release fanfare (A4→C#5→E5 — the A-major triad). THE ONE-KEY CONSONANCE LAW: every cue lives in the A family, so the same-moment fanfare+chime overlap (a release also cleans its repo) is consonant BY CONSTRUCTION — channel independence preserved musically. Opt-in via ONE new localStorage key `katlab.sound` (default OFF, the katlab.notify precedent), toggled beside the OS-alerts switch + a palette twin; visibility-INDEPENDENT (the background-awareness channel — the v0.1.7.0 channel matrix's 4th column: you HEAR Claude working while reading elsewhere); the gesture-law AudioContext (create/resume only from the toggle click or the one-time first-interaction listener on persisted-on reloads), feature-detected (no AudioContext → toggle honestly inert-off), the RV15 enable order (key write → resume → confirmation tick at A4); Legend line added
- **Weather Forecast** (B.1/R-BJ): `weatherOf` gains its 5th state — "forecast", the gathering storm in the symbolic (rain − 12h, rain] window (`WX_FORECAST_LEAD_H` beside the fn; both bounds self-adjust against the ONE-source `UNCOMMITTED_AGE_H`): ONE still lighter cloud (no drops, no motion, zero new CSS — reduced-motion-immune by construction), attrs-only in the pure scene (rides the snapshot), with the ONE computed weather tip "rain in ~Nh — commit to clear" (N = max(1, ceil) — never "~0h" at the exact-threshold edge, which is still forecast since rain needs strict >48h); `WEATHER_TIP`'s type narrows to exclude forecast (the map honestly stays three-keyed, the title builder branches three ways); the nudge law: the forecast NEVER notifies/sounds/bells — weather forecasts, it does not demand
- **Engineering discipline**: battery 31/31 on the real sources (esbuild-bundled: the five weather states + every boundary incl. the exact-48h forecast edge; the pitch hash's determinism + anti-ladder over 30 sequential ids + THE DISCRIMINATOR row — the low-bits form fails at ≥90% ladder-ish while the shipped form passes; the drop-limiter; the exact pinned NOTES/quintet tables; play* callable in node with zero AudioContext — the feature-detect path executed); the served bundle artifact-grepped per the v0.2.7.0 CFT-2 law (hash constant + forecast tip + sound key all present); tsc + vite green, py_compile green, exactly ONE new localStorage key (6 total)
- **Post-implementation CFT loop** (2026-08-16): 5 fresh passes, **ZERO product findings** — the third zero-finding CFT in house history (after v0.1.12.0 and v0.2.2.0) and the first for a release carrying a whole new module: the 21-pass review's 17 pre-implementation fixes left nothing to catch. Executed proof throughout: battery re-run on the final tree, dist freshness mtime-proven, served bundle artifact-grepped, live sweeps clean
- **Scope:** backend diff = version.py ONLY; no schema/endpoint/hook/git-call change; zero new pip/npm deps; cross-repo contract unchanged

## v0.2.7.0 — Alive & Rewarded (2026-08-15)

**Key Highlights**

- Twentieth harvest cycle — user agreed with the recommended trio (2026-08-13): R-BE City Weather + R-BF Kat's Wardrobe + R-BG Release Fireworks as ONE all-frontend release. Plan passed a 23-pass 4-step CDD review loop (25 findings RV1-RV25 fixed in place, 5/5 consecutive clean) with the heaviest traps caught pre-implementation: RV5 the RELEASE-LINE LAW (every KATLAB commit is version-ENDed — live-proven 10/10; a bare regex fire would banner every ordinary commit, so the moment fires on a 3-PART-PREFIX change only, per the house Release-repo fast-forward convention), RV18 the at-mount seed execution-order bug (repos is EMPTY at mount — the session's first real release would silently seed instead of fire), RV14 the seed-clobber race (only-if-null writes), and the RV6/RV7 misleading-precedent pair (a view-gated banner would die on 4 of 5 views; display:none weather would hide health truth from reduced-motion users)
- **City Weather** (A.1/R-BE — genuinely novel, no prior art in the city-metaphor literature): repo HEALTH as weather in the pure CityScene svg — rain (cloud + 8 falling drops) over a district whose uncommitted work ages past the ONE-source `UNCOMMITTED_AGE_H` chain (bell nudge + Kat "anxious" + now this), sun (glow disc + 8 rays) over CLEAN — rendered as the MOON at the night sky-bucket (a sun at 2am breaks the sky fiction), fog drift-rects over offline; FRESH uncommitted = deliberately NO weather (states, not work). Laws: scene-not-overlay (weather RIDES the snapshot — hiding rain would lie about health), fill/stroke attrs only (`FILL_MAP`/`inlineSvgClasses` untouched by construction), index-math geometry (the stars law), wx-* motion in the ONE reduced-motion block as `animation: none` (state stays visible), pointer-events off (weather never blocks building clicks); the platform tooltip gains the weather word + the bell's wording
- **Kat's Wardrobe** (B.1-B.2/R-BF — the vscode-pets class deepened): EARNED accessories derived fresh every render from the served UNSCOPED 365d calendar via `wardrobeOf` (imports `maxStreakOf`/`bestRolling7` from records.tsx — one truth; the pet MIRROR law survives whole: zero persistence, zero interaction, zero localStorage — delete the DB, lose the wardrobe). Tiers (`WARDROBE_TIERS`, one home): collar 7-day / bandana 21-day / crown 50-day streak, scarf 500-capture week, star 300-capture day — live at ship: collar+scarf+star ON (streak 14 · week 679 · day 312), the tip shows earned + the RV20-deterministic next ("next: bandana at a 21-day streak"). Fed by ONE 60s-throttled unscoped `api.stats()` in App (the v0.2.0.0 City-RV3 recipe) threaded to all THREE Kat sites — header chip, focus wall, the City street (via CityView's HTML overlay — the pure scene and its snapshot never see it); accessories ride pet-breathe, mood faces untouched (the star pins the LEFT chest, clear of every mouth variant)
- **Release Fireworks** (C.1/R-BG): a version-END commit that CHANGES the 3-part prefix fires the moment — detection is client-side on the already-streamed `commit_detected` payload (swept variants guarded; per-session hash second belt) against a baseline seeded on the FIRST non-empty repos payload (one one-row history fetch per repo, once-latched, only-if-null). The moment: a z-40 full-width banner STACK under the header (top-level, EVERY view; per-repo entries newest-first, cap 3, same-repo replaces, own 12s nonce-compare dismiss each) with `open changelog ↗` (the built site's live page; the story page honestly deferred to "the Scribe drafts the release notes on its next daily tick"), the 24-particle hero burst (reduced-motion gated), and the new `notifyRelease` trigger (4) in notify.ts (hidden-only via the private `fire()` — the v0.1.5.0 D5 background-only law; the banner covers the visible tab). `RELEASE_RX` + `prefix3` live in theme.ts with the scribe.py mirror-contract comment (the v0.2.5.0 D10 message-END law — both ends, one law); documented edges: same-repo CLEAN-toast stacking layered by design, the WS-gap law (a missed release banners later under the line's current version), the changelog's one-tick lag self-healed by refresh.js
- **Engineering discipline**: machine battery 45/45 on the REAL sources (esbuild-bundled: weatherOf/wardrobeOf/RELEASE_RX/prefix3 truth tables incl. every tier boundary + the real EA/UM subjects + the real UM rider chain staying quiet; CityScene render battery — rain/sun/moon/fog/fresh node counts + attrs, zero new class-based fills, snapshot class-strip with weather surviving as attrs; the handler-sim — fire-once, rider-silent, null-seed, swept-guard, the RV14 no-clobber); V3 live parity exact (14/679/312); tsc + vite GREEN first compile; V6 proofs — the 8-file CODE diff exactly the planned code set (the D.1 docs ripple joined after, CLAUDE.md deliberately untouched — no run-surface change), zero new deps, zero new localStorage keys, `UNCOMMITTED_AGE_H` imported never copied, no Math.random at render
- **Post-implementation CFT loop** (2026-08-16): 8 fresh passes, 3 findings CFT-1..3 fixed in place, ending 5/5 consecutive clean — CFT-1 `overflow-hidden` clipped the hero burst to the ~36px banner band (removed; particles fly free per the chip-burst precedent); CFT-2 the CFT-1 fix's own JSX-comment syntax error compounded by a pipe-masked "green" rebuild (`npm run build | tail` reports tail's exit) — caught by the served-bundle truth check, minting the build-claim law: the builder's own exit code + an artifact grep, never a pipeline exit; CFT-3 a stale-able file-count enumeration in these very notes (the v0.2.6.0 CFT-7 class). Closed with the battery re-executed 35/35 on the final tree and dist freshness mtime-proven
- **Scope:** backend diff = version.py ONLY (the house pride held); no schema/endpoint/hook/git-call change; zero new pip/npm deps; cross-repo contract unchanged

## v0.2.6.0 — One System (2026-08-12)

**Key Highlights**

- Nineteenth harvest cycle — user-ruled scope ("UI buttons + combine everything into one" + "the cmd window spam makes me tired"): ALL FOUR candidates shipped (R-BA one-port merge · R-BB in-app view · R-BC lifecycle fold · R-BD silent ops), 8200 retired. Plan passed a 48-pass CDD review loop (32 findings RV1-RV32 fixed in place, 5/5 consecutive clean) with the three heaviest traps LIVE-PROVEN pre-implementation: the venv/system interpreter split ("No module named mkdocs" in the venv — spawn/probe pinned to the PATH python, never sys.executable), FastAPI's no-auto-HEAD (405-probed — the resolver registers GET+HEAD explicitly or both self-healing mechanisms die), and the fresh-clone missing `data\logs` (the silent launch's redirection fails on a missing dir)
- **One port** (A.1/R-BA): the tracker itself serves the BUILT Chronicle at `/chronicle/` — a per-request FileResponse resolver (the v0.1.12.0 no-StaticFiles law: picks up a late-built site live, never raises on a fresh clone), traversal-guarded (404 never 500), pinned content types, UNIFORM no-cache (the C6 rationale applies to unhashed assets exactly as to pages), the self-healing not-built 404 (meta-refresh 5s — any landing enters the site the moment it exists), `/chronicle` → `/chronicle/` (relative-link law), and mkdocs' own absolute-path 404.html never served
- **One process** (A.2/R-BC): the tracker's lifespan OWNS the Chronicle regen loop (guarded spawn: demo never, mkdocs-absent → one log line and the tracker is unaffected; clean shutdown terminates it; the stop bat's tree-kill and the loop's tracker-port self-exit are the second and third belts); the loop's lock gains the crash-window RETRY (~30s × 15min — a crashed tracker's orphan can never leave a restarted tracker loop-less); `mkdocs serve` is RETIRED and its entire all-404 failure class (v0.2.4.0 CFT-5/CFT-6) dies with it — builds go to `site.new` then swap ATOMICALLY (rename-first: the old site serves WHOLE through any failure)
- **Living freshness** (B.1): livereload replaced by a generated `refresh.js` — a 15s visible-tab HEAD poll of the site index's Last-Modified (protocol-guarded: file:// no-ops; null-guarded: a mid-swap 404 never triggers a reload into a dead end); `site_url` composes from the config (F10); the one-shot `generate.bat` now also builds
- **In-app Chronicle** (C.1/R-BB, R-AY absorbed): the 5th top-level view — a same-origin iframe with the honest not-built card (re-probing every 10s, flips itself when the first build lands), the header 📖 chip, palette entries ("Open Chronicle view" + "Open Chronicle in a new tab ↗"), and the Legend line incl. the focus-boundary note (shortcuts pause while the embedded site holds focus — documented, not a bug)
- **Silent ops** (E.1/R-BD): ZERO persistent console windows — the start bat launches the server via `pythonw` with the load-bearing wrapper incantation (`start "" /b cmd /c "… >> data\logs\tracker.log 2>&1"` — `start` alone eats redirection and crashes pythonw's bare handles; a plain call waits forever), rotates both logs (one previous session kept), guards against double-starts (already-running → just opens the browser), creates `data\logs` first, and opens the browser at the config-read port; the demo trio silenced identically (`demo.log`); uvicorn access logs retired (the loop's own ~11.5k daily REST lines would have drowned the log — app-level + error logs stay); `serve.bat`, `live.bat`, `_start_all.bat` DELETED; LIVE-CAUGHT postscript (user: "blinking windows"): hidden ≠ silent — under a windowless `pythonw` parent every UNFLAGGED console child allocates a fresh conhost that FLASHES on screen (the git poll made it a strobe, the startup mkdocs probe flashed once more), so ALL FOUR child-spawn sites (git_module `_run` · main.py mkdocs probe · generate.py mkdocs build · scribe.py claude) now pass `CREATE_NO_WINDOW` — safe by construction, every site pipes its output
- **Scope:** backend = main.py (resolver + spawn) + git_module.py (the R-BD no-window flag) + version.py — the first backend logic since /api/health; zero new pip deps; generate.py/scribe.py touched for the R-BD flag; the post-impl CFT loop's fixes rode the same version across main.py, pages.py and the KEPT Chronicle bats (see the plan CFT LOG); cross-repo contract unchanged
- **Post-implementation CFT loop** (2026-08-12): 18 fresh passes, 14 findings CFT-1..CFT-14 fixed in place, ending 5/5 consecutive clean — incl. TWO real code bugs that post-dated the 48-pass pre-impl review (CFT-13: the RV16 lock retry's exists()+stat() TOCTOU crash on the orphan's designed unlink gap; CFT-14: the pre-yield loop-spawn Popen unguarded — a spawn OSError would have killed the tracker over its OPTIONAL child), the bare-dir 307 (CFT-5), the `-u` unbuffered loop log (CFT-6), view.bat decoupled to site.view (CFT-4), and the serve/live residue + doc-truth sweep; resolver edge battery (traversal x4 forms, reserved device names, 404-mask, HEAD+Last-Modified) proven live on the deployed process

## v0.2.5.0 — The Scribe (2026-08-11)

**Key Highlights**

- Eighteenth harvest cycle — "auto-doc-ing?" (user 2026-08-10): the AI-devlog class (Devlog CLI / GitDiary / DevDiary / gitmore) all GUESS the why from raw commits; the Scribe inverts the input — Claude Code HEADLESS (`claude -p`, subscription login, NEVER `--bare` — bare skips OAuth) is fed the tracker's KNOWN why via REST. Plan passed a 32-pass CDD review loop (48 findings RV1-RV48 fixed in place, 5/5 consecutive clean) run as a /loop 5m fresh-eyes cycle; user rulings: daily-once cadence, all three outputs, haiku default
- **The Scribe** (A.1-A.2): `Scripts/Chronicle/scribe.py` — stdlib+subprocess module writing three page kinds into gitignored `Chronicle/runtime/docs/story/`: daily diary (completed UTC day), weekly retro (completed ISO week; isocalendar-YEAR filenames — the Jan-1 straddle law), release-notes DRAFTS (message-END 4-part version commits of the MONITORED repos; TM isn't self-monitored). Engine: stdin JSON data doc + instruction prompt (`--output-format json`), the 5-CONDITION validation gate (exit 0 · RV15 outermost-JSON parse · is_error false · ≥200 chars · RV28 "](" link reject), the RV37 communicate-retry spawn (drain-safe vs the Windows ~64KB pipe deadlock; per-retry LOCK HEARTBEAT so a long spawn can never re-open the v0.2.4.0 CFT-2 stale-takeover hole; stderr=DEVNULL; input on the first call only), validated-result-or-nothing ATOMIC UTF-8 writes with the AI-authorship footer (the one scoped wall-clock exception — story pages are existence-gated, never content-diffed)
- **Quota laws** (the money guards): ONCE-EVER by existence (delete or `--force` to regenerate; `--force` never overrides COMPLETENESS), ≤1 invocation per loop tick with deterministic drain order (diary oldest-first → weekly → release), 3 attempts/target/process, 7-day horizon, and the GLOBAL 5-runs/UTC-day cap (`.scribe_runs.json` at the runtime root: fail-CLOSED on corruption — the capture hook's fail-open deliberately inverted, assemble→reserve→invoke with NO refunds; the ±1 and same-target manual-vs-auto races documented-accepted)
- **Honesty laws**: RV44 two-basis minutes — per-repo scoped figures are NEVER summed, totals read the UNSCOPED calendar (the 649-vs-375 non-additivity can't reach prose); RV14 untrusted-data framing (whys/messages are content to describe, never instructions); RV39 write-time snapshot scoping (catch-up-ingest drift documented; delete-to-regenerate heals); RV25 no-data pre-skip (zero-spend)
- **Site integration** (B.1): generate.py stamps the model PRE-FETCH (`_fetched_at`, internal — the RV31 gate: no midnight-race minute understatement), lazily imports scribe inside loop() (RV22 — one-shot generate NEVER scribes, import-level true) and passes its lock-utime callable; the nav gains a "Story" section from the RECOGNIZED-pattern scan (index.md excluded — RV41's self-sustaining-index trap; alien files out of contract — RV42; diaries cap-14 + `pages.build_story_index`; hidden at ZERO pages with the generate-OWNED index emitted-or-removed — RV35)
- **Scope:** backend diff = version.py ONLY; zero new pip deps (the claude CLI is a system tool, not a dependency); no UI/schema/endpoint/git-call change; cross-repo contract unchanged; `scribe.bat` = the double-click manual drain (F54 message discipline)

## v0.2.4.0 — The Chronicle (2026-08-06)

**Key Highlights**

- Seventeenth harvest cycle — "Focus on MkDocs" (user 2026-08-04): the Live_arch doc tool identified as MkDocs 1.6.1; the git-cliff/devlog-cli auto-docs class beaten on data (they GUESS the why; the tracker KNOWS it). Plan passed a 19-pass CDD review loop (37 findings RV1-RV37, 5/5 consecutive clean incl. live-data proofs: RV9 stale refs exist in production, RV17 non-additivity 649-vs-375 min, 29-file mirror link audit zero 404s)
- **The Chronicle** (A.1-B.1): `Scripts/Chronicle/generate.py` + `pages.py` — stdlib-only generator against the RUNNING server's REST (D2; envelope unwrap, pages of 500 cap 20, ~10s timeouts RV34, UTF-8 both ends RV33, ALL fetches before the first write RV10) emitting a full MkDocs site into gitignored `Chronicle/runtime/`: Overview (status cards + 365d totals + the ONLY data-through stamp — RV29), Devlog (page per active UTC day; whys, files, served ≈minutes, commits, "(unattributed)" bucket RV32), Plans (RV8 path-flattened filenames + RV37 full-path nav labels, plan-board sort), Changelog-by-reason (RV9 stale-ref law live-proven), Docs mirror (byte-verbatim incl. LICENSE — RV5) + temp/Ref mermaid diagrams page; write-only-if-changed ATOMIC sink (RV27) + FULL-expected-set sweep (RV14/RV22) + calendar-diff incremental ticks (RV19 — catch-up-ingest safe); md/YAML escape laws (D10/RV30)
- **The stack** (C.1): MkDocs 1.6.1 + mermaid2 + panzoom + pymdownx (own requirements.txt, plain pip — user-agreed; product deps untouched), stock theme restyled Bootswatch-Darkly at the CSS layer (T5) + the generated KATLAB product-look stylesheet (IMPL-3, user-requested polish: app fonts with offline-safe fallbacks, slate-950 + teal glow, card lists, dark tables/nav); the four Live_arch proven tricks harvested verbatim: T1 use_directory_urls:false (file:// view), T2 UMD mermaid (never CDN ESM), T3 explicit --livereload (the click>=8.2 trap), T6 fence_mermaid superfences form (RV16 — blank-diagram trap on non-Material themes), + the GENERATED fix_windows_paths build hook (IMPL-1: mermaid2's os.path.relpath emits BACKSLASHED script paths on Windows — the Live_arch hook lesson, caught live post-build); five bats: install/generate/serve/live/view with RV25 guards, + `Scripts/_start_all.bat` (IMPL-5, user-requested: ONE double-click starts the tracker if 8100 is dark then the Chronicle living site; underscore prefix sorts it on top), RV13 existing-site fallback, RV26 loop lifecycle (own titled window, heartbeat lock at runtime root RV28, self-exit on dark serve port), port 8200 (D4)
- **Scope:** backend diff = version.py ONLY; no UI change (palette entry backlogged as R-AY, user 2026-08-06); no new endpoints/schema/git calls; cross-repo contract unchanged; gitignore + Chronicle module + docs are the whole diff

## v0.2.3.0 — Editor & Health (2026-08-04)

**Key Highlights**

- Sixteenth harvest cycle — the Live_arch RE-mine (source-links.js "nothing is guessed" + the audit scripts' "an orphaned page is worse than a missing one"); the folder is now MINED OUT/retired for product ideas; plan passed a 16-pass CDD review loop (12 findings RV1-RV12, 5/5 consecutive clean)
- **Open in editor** (D1/B.1): the ONE `editor ↗` anchor chip in the FileStory header — the convergence point every file chip already opens (plan board, coupling, treemap, changes-by-task, city buildings; RV1: NOT the palette). `repoPath` prop threaded via the repoBranch precedent; href = `vscode://file/` + encodeURI(absPath with all backslashes normalized) + the post-encode `#`→`%23` (RV6 — `#` is legal on Windows and encodeURI leaves it raw; `?` needs nothing, Windows forbids it); null-guard hides the chip; VS Code user-confirmed as THE editor; honest caveat in the notes (protocol handler required)
- **Health panel** (D2/A.1+B.2): `GET /api/health` in routes.py — READ-ONLY self-audit (zero git calls, zero DB writes; the first REST addition since /badge v0.2.0.1): server block (version, STARTED_TS import-time stamp, guarded DB_PATH.stat, watchers alive/total from the tracker._tasks done() scan, hook = never-raising TEXT scan of ~/.claude/settings.json for the hook line + the checked path) + per-repo block in config order (db.get_last_event_ts — the D9 heartbeat read /repos uses (RV2), guarded stat of the EXISTING repo.events_file property (RV3/IMPL-1) with mtime serialized ISO-Z to match last_event_ts (RV9), warning COUNTS only — the F47 banner keeps the surface (F5)). Frontend `healthPanel.tsx`: neutral "sys" header chip (no polling, no dot) + modal (fetch-at-open per the RV17 rule, STABLE onClose) with EXPORTED presentational HealthBody({data}) (RV8 — the battery shape); rows: uptime/DB-size ("—" on modeled null — RV12) / watchers green-red / hook "line present ✓/line missing ✗" (RV10 — the honest presence-only wording) / capture freshness "12m ago"/"never" / jsonl size with the last-log-write title attr = the INGEST-LAG hover (RV5; suppressed when null — RV12) / warning counts muted-0/amber; palette entry "Open system health" under Views
- **Scope:** backend touch = version.py + routes.py ONLY; zero new deps; no new localStorage keys; cross-repo contract unchanged

## v0.2.2.0 — The Plan Board (2026-08-03)

**Key Highlights**

- Fifteenth harvest cycle — sourced from the Live_arch mine (the EA repo's living-architecture site: the developing.md/now.md "current work at the top" philosophy mapped onto the served tasks); plan passed a 7-pass CDD review loop (3 findings RV1-RV3, 5/5 consecutive clean); NOT taken from the mine (standing rejections hold): LikeC4/C4 (logic map), the light-theme toggle, docs linters
- **The Plan Board** (D1/B.1): `planBoard.tsx` — `groupActivePlans` (exported, battery-proven) groups the tab-scoped tasks by (repo, plan_file); ACTIVE = ≥1 non-done task (done-only plans never render; zero actives hides the card — the coupling precedent); sort = max(last_event_ts) DESC with nulls LAST + deterministic ties; per plan: basename header (both path separators) + repo chip + done/total + a segmented bar (done teal / in-progress amber riding the pulse-dot class — already in the ONE reduced-motion block / pending slate, served order); the SPOTLIGHT renders the SHORT task id via split(" - ").pop() (RV3 — the KpiRow busiest-task precedent; full refs ride title attrs) + title + the 2-line-clamped why + up to 4 file chips ("+N") opening FileStory; NEXT UP = the first pending (short id); the no-in-progress state renders honestly ("no task in progress" — the discipline guard keeps the nagging). PLACEMENT (RV1): a SIBLING of the KpiRow line BEFORE both totalEvents branches — task-driven, never event-gated (a fresh repo with plans shows missions pre-capture); battery 16/16
- **Hygiene** (D2/D.1): `npm update postcss` → 8.5.25 (≥ the 8.5.18 Dependabot-HIGH patch floor; nanoid 3.3.17 rode along inside postcss's own tree) — the LOCKFILE-only diff, package.json byte-identical; build GREEN after; exposure was ~nil (dev-scope, build-time, own CSS only) — hygiene, not an active risk
- **Scope:** all-frontend + the lockfile bump (version.py the only Backend touch), zero NEW deps, no new localStorage keys, cross-repo contract unchanged

## v0.2.1.0 — Records & Snapshots (2026-08-02)

**Key Highlights**

- Fourteenth harvest cycle (Strava Best-Efforts class · Tinycon dynamic-favicon class · "share your city"); the FOURTH all-frontend release (backend diff = version.py only); plan passed a 6-pass CDD review loop (2 findings RV1-RV2, 5/5 consecutive clean) with the record math proven LIVE pre-implementation: maxStreakOf 11 vs streakOf 9 — the max-run/current-run split is necessary on real data — and the snapshot's class-inlining transform proven on real CityScene markup (15 class attrs → 0, idempotent)
- **Personal records** (D1/B.1): `records.tsx` — four rows from the served calendar (best day captures/effort, longest streak via the new exported `maxStreakOf`, best rolling 7d week via `bestRolling7`); DISPLAY spans the full calendar while DETECTION compares today against the record-to-beat over calendar[:-1] (today excluded) with the STRICTLY-GREATER law (ties never fire; a break fires exactly once via the prev-raw-values crossing — the goalRings recipe); break → the ~12-particle celebration burst on the row + a ~4s "NEW RECORD 🏆" banner (nonce-compare + unmount clears); the instance is scope-keyed at the call site; "(last 365d, UTC)" honesty heading; the ARCADE SHELF created (RV1): a new lg:grid-cols-2 row wraps TrophyCase | Records (trophies first — a deliberate documented placement change); battery 12/12
- **Live status favicon** (D2/B.2): `favicon.ts` — drawStatusFavicon canvas → data-URL (teal ring on slate-950 CLEAN / amber dot + clamped count dirty; `clampCount` exported, >99 → "99+"); one App effect on [repos] swaps the rel="icon" link href/type — the basis expression is the app-badge line VERBATIM (grep-proven ×2: the presence surfaces never disagree); feature-detected silent no-op; the static favicon.svg stays for cold loads; manifest icons untouched; Safari ignores dynamic favicons — documented
- **City snapshot** (D3/B.3): the City h2 gains the Overview flex treatment (RV2) + a "snapshot ⬇" button; the exported `inlineSvgClasses` pure transform maps the scene's four fill classes to hexes and strips ALL class attributes (a standalone .svg has no Tailwind — text would be invisible otherwise), XML declaration + Blob → `KATLAB_City_<date>.svg`; the rain/fireworks/Kat live on the HTML overlay — excluded by construction; battery 5/5 on the shipped transform over real markup
- **Scope:** all-frontend (version.py only), zero new deps, no new localStorage keys, cross-repo contract unchanged

## v0.2.0.1 — Report & Badge (2026-08-02)

**Key Highlights**

- Thirteenth harvest cycle (git-wrapped generator class · github-readme-stats class); plan passed a 14-pass CDD review loop (7 findings RV1-RV7, 5/5 consecutive clean) — the finding themes were HONESTY pins: RV2 three report sections are served ALL-TIME (punch card, identity, effort/churn top tables) and now carry explicit "(all-time)" heading markers inside a range document; RV5 the hero's commits figure reads sum(calendar[-N:].commits), NEVER the all-time stats/identity commits (battery poison fixture: 3 vs 999,999); RV6 the badge XML-escapes the interpolated repo id (parser round-trip proven); RV1 the palette entries JOIN the existing "Actions" section beside the digest (never a new section)
- **The Report** (D1/B.1): `reportHtml.ts` — a string-builder document (the digest.ts recipe grown up: dark inline CSS, the fonts-with-fallback precedent, Blob download named `KATLAB_Report_<scope|ALL>_<7d|30d>_<date>.html`); consumes STATS + SCOPE ONLY (RV7 — no repos/tasks inputs); ranges 7d/30d as calendar-tail projections; sections: range-true hero (4 calendar sums) → momentum (the strip's delta law VERBATIM + a 2N-point sparkline, prior half 0.35) → RAMP-bucketed daily strip (range max) → all-time rhythm punch mini → all-time identity donut (stroke-dasharray arcs) → all-time top-5 tables (churn scope-filtered) → wrapped on the 7d report ONLY (served as last-7-UTC-days, never stretched) → footer "KATLAB TrackingMonitor — generated <local ts>" with NO version claim (RV3); entries: the Overview "Report ⬇" button (7d current scope) + two Actions palette entries, all no-ops while stats is null; battery 15/15
- **The Badge** (D2/B.2): `Backend/app/badge.py` + ONE main.py root route GET `/badge/{repo_id}.svg` (OUTSIDE the FRONTEND_DIST guard — server-rendered; unknown repo → 404; image/svg+xml + Cache-Control max-age=300): a 380x80 slate-950 card with the teal ring mark, the repo id (XML-escaped, RV6), "N events · last 7d (UTC)" via `{:,}`, and the StatusBar-palette status chip; data = the tracker's in-memory status + ONE read-only COUNT via the THREAD-LOCAL get_conn (db.py byte-untouched; the substr day-bucket law — review-proven parity 40=40/302=302); python battery 9/9. HONEST LIMIT everywhere it matters: github.com's camo cannot reach 127.0.0.1 — the badge lives in LOCAL previews (VS Code)
- **Cross-repo:** Installation_Guideline.md + Onboarding_Prompt.txt gain the OPTIONAL README-badge section (the first contract ADDITION since v0.1.10.0 — optional, never required; each repo's own AI decides)
- **Scope:** backend diff = version.py + main.py (one route) + the new badge.py; no schema, no existing-endpoint change, no new git calls; zero new deps; no new localStorage keys

## v0.2.0.0 — KATLAB City (2026-08-02)

**Key Highlights**

- Twelfth harvest cycle — THE BREAKTHROUGH release (user bar: "a super wow feature or a breakthrough point to bring it to v0.2.0.0"); GitCity/Codebase City/Gource class but ALIVE; plan passed a 15-pass CDD review loop (6 findings RV1-RV6, 5/5 consecutive clean) with RV1 caught by a LIVE probe pre-implementation: the served GLOBAL top-20 file_churn splits 13/7/0 across repos — a busy repo starves quiet districts BY THE CAP (EA_Dev: 7 global rows vs 20 scoped), so the city fetches `api.stats(repo.id)` PER configured repo (Promise.all, alive flag; the tab-scoped App stats prop serves only as the freshness nonce), THROTTLED to one round per 10s with ONE trailing catch-up (RV3 — get_stats is the heavy endpoint and the nonce fires per WS sync; state-machine simulated: bursts collapse, the final state always lands)
- **KATLAB City** (D1-D3/B.1-B.3): `city.tsx` — the 4th top-level view (View union + nav TabButton + palette entry, withViewTransition verbatim), workspace-wide by design (full repos/tasks/events props, never tab-filtered). STATICS: one iso district per repo (module-private 2:1 dimetric math — the skyline recipe at city scale; PITCH 190, height 340, overflow-x-auto), buildings = each repo's OWN scoped top-20 churn (cap 8, churn-desc; height 14 + sqrt(events/churnMax)·64 with churnMax across ALL districts — comparable; tint = the treemap recency bands verbatim on the shared RAMP; painter back-to-front; 3-face iso boxes with the skyline shade helper), zero-churn plaza, plaques mirror the StatusBar palette exactly (CLEAN ✓ emerald-600 / N uncommitted amber-500 / OFFLINE zinc-600; offline district 0.5 opacity + "z z"; CLEAN ground emerald-950), cranes = "in-progress" tasks (cap 3 + "+N", task-ref tooltips), building tooltips = the treemap content, click-throughs: building → FileStory, plaque → onGoRepo (setTab + setView("overview") in ONE transition)
- **Liveliness** (D2/B.2): event RAIN — id-keyed growth diff of the events prop vs a prevIds ref (first payload = baseline; pool shrink silent), one teal `city-drop` particle per new row over its district (newest ~12; per-BATCH prune timeouts removing only their own batch's ids — RV6, ids not nonces; all cleared on unmount); commit FIREWORKS — repos VALUES-ref diff (the rings precedent), count DROP → the ~12-particle celebration burst above the district (independent per-district nonces, ~900ms nonce-compare clears); day/night sky by LOCAL-hour half-open buckets ([22,5) night / [5,8) dawn / [8,17) day / [17,22) dusk, slate hues only) + 18 DETERMINISTIC star dots at night (index-math seeded — never Math.random at render; two renders byte-identical); Kat walks the street (`kat-walk` ~45s alternate, the SAME App-computed mood); city-drop + kat-walk + burst-p all under the ONE prefers-reduced-motion media block — reduced renders a still model
- **Batteries:** statics 20/20 (heights/cap/tints at both band edges, all three plaque states, plaza, offline, crane cap, deterministic stars, sky boundaries), rain-diff 5/5 semantics (exact code expressions), fireworks 4-case + throttle state machine (review-executed), tsc clean, build GREEN
- **Scope:** ZERO backend (version.py only), zero new deps, no new localStorage keys; the v0.2 line marks the product milestone — the cross-repo contract is UNCHANGED (last contract change: v0.1.10.0)

## v0.1.13.0 — Snake, Pet & Clock (2026-07-31)

**Key Highlights**

- Eleventh harvest cycle (Platane/snk class · vscode-pets class · polar-dial class); plan passed a 20-pass CDD review loop (13 findings RV1-RV13, 5/5 consecutive clean) with TWO delight defects caught by LIVE simulation pre-implementation: a col-0 snake would crawl ~93% dead grid on the young install (RV9 → start at the first non-zero column) and the fixed 21-position path at a flat 24/s would flash by in ~0.9s (RV10 → speed = min(24, pathLength/4), a ~4s minimum show — the head reaches the last position at (pathLen-1)/speed ≈ 3.8s on the live 21-position path); plus RV2 (a bare combo-count check would leave the pet excited FOREVER — comboCount never time-decays, so "excited" mirrors the ComboMeter's exact liveness window) and RV13 (a strict always-5 segment fixture would fail correct grow-in code)
- **Activity snake** (D1/B.1): `snakeGame.tsx` — third calendar view (`flat | city | snake`, legacy localStorage values safe); module-private heatmap grid math + IMPORTED RAMP/rampBucket (single color source); column-major serpentine from the FIRST non-zero column (col-0 fallback; direction parity by ABSOLUTE column index), eaten = DERIVED (path index <= head index, no per-cell state); head + up-to-4 segments growing min(5, k+1); rAF virtual-time engine with clamped dt, calendar SNAPSHOT per run (stats ticks apply next run), auto-play on view entry + ▶ re-run; end caption "year devoured — N events 🐍"; reduced motion = static grid + parked head, control hidden (JS guard — rAF-driven); battery 16/16
- **Workspace pet "Kat"** (D2/B.2): `pet.tsx` — pure exported `moodOf(repos, comboCount, comboLastMs, nowMs)` with the 5-mood priority model (excited LIVE-combo-only > sleeping all-offline > content all-clean > anxious 48h+ aged > curious); `UNCOMMITTED_AGE_H` LIFTED App.tsx → theme.ts (ONE source for the bell nudge AND the pet — bell condition byte-identical); mini SVG cat with five distinct mood faces; breathing/blink/bounce keyframes joining the ONE index.css prefers-reduced-motion media block (the always-mounted-CSS mechanism; the curious 4° tilt is a static transform and deliberately survives it); header chip immediately LEFT of the ComboMeter (the arcade cluster) + big on the focus wall via a new FocusMode `mood` prop (App computes the mood once per render, combo timestamp ref-read like the ComboMeter feed); battery 19/19 incl. both combo-window boundary sides and the exactly-48h strict edge
- **Today's radial clock** (D3/B.3): inside `dayLanes.tsx` — `lanes | clock` toggle (persisted `katlab.dayView`, default lanes; the clock button DISABLED while replay is armed); 24 LOCAL-hour wedges from the SAME fetched rows (zero new fetches), outer radius 26 + sqrt(count/dayMax)·84 (area honesty), fills via the shared RAMP bucket, zero hours as hairline ticks, labels 0/6/12/18, per-wedge padded tooltips, center = day + en-US total + "(local)"; live teal hand only when the picked day IS today (render-computed — the P8 60s tick moves it, zero new timers); empty day renders NO dial (the shared "No captures this day." paragraph covers it — the lanes' exact svg-gate mirror); battery 10/10
- **Scope:** ZERO backend (version.py only — no schema, API, git-call, or hook change), zero new deps; `katlab.calendarView` gains "snake", `katlab.dayView` new; the Installation_Guideline/Onboarding_Prompt need NOTHING (no cross-repo contract change)

## v0.1.12.0 — Momentum & Presence (2026-07-29)

**Key Highlights**

- Tenth harvest cycle (KPI-delta dashboard staple · Badging API class); plan passed an 8-pass CDD review loop (3 findings RV1-RV3, 5/5 consecutive clean) with the delta math dry-run-proven on the LIVE DB (474 vs 512 → "▼ 7%") and TWO hazards designed out pre-implementation: the `/icons` StaticFiles mount (raises at CONSTRUCTION on a missing dir — a stale pre-v0.1.12.0 dist passes the FRONTEND_DIST guard yet would kill the server at startup, since the start script rebuilds only when `dist/index.html` is absent → explicit per-file routes instead) and the manifest background hex (body is slate-950 `#020617`, header slate-900 `#0f172a` — the two manifest color fields DIFFER by design)
- **Momentum strip** (D1/B.1): `momentum.tsx` — this week = calendar `[-7:]` (today inclusive, the wrapped precedent), last week = `[-14:-7]`; per-metric window sums (events/minutes/commits); the HONEST delta rule (prev>0 → rounded pct ▲/▼/=; prev==0 && cur>0 → "new ▲" — a percentage against zero is a lie; both 0 → "—"); ▼ renders SLATE, not red (a light week is not a failure); three tiles with the goal-rings metric palette (teal/sky/amber), each a 14-day module-private SVG polyline pair sharing point 6 (prior week opacity 0.35, current bright, max-normalized, flat baseline at all-zero); tooltips carry both exact sums; placed between KpiRow and the charts grid inside the totalEvents gate; battery 18/18 incl. poison-exclusion (slice(-14)) and all four delta branches
- **PWA shell** (D2/B.2): `manifest.webmanifest` (standalone; background `#020617` / theme `#0f172a` — RV3; 192+512 PNG icons), `Scripts/gen_pwa_icons.py` (stdlib zlib/struct PNG writer — slate-950 rounded square + teal ring, the goal-rings mark; PNGs COMMITTED, round-trip validated), `sw.js` = the minimal installability worker (skipWaiting / clients.claim / NETWORK-ONLY fetch — no Cache Storage ever, the C6 no-cache rule never fights a cache; fetch handlers never intercept ws:// so /ws is untouched), index.html manifest link + theme-color meta, main.tsx feature-detected registration (silent catch); main.py gains four explicit FileResponse routes inside the FRONTEND_DIST guard (manifest as application/manifest+json; sw.js as application/javascript + no-cache — the stale-SW trap; both icons as image/png; NO StaticFiles mount — RV2)
- **App badge** (D2/C.1): one `[repos]` effect — n = SUM(count) over NON-OFFLINE repos, EXACTLY the ALL-tab uncommitted KPI basis (RV1: an offline repo's stale count must never pin a wrong number to the taskbar; the OFFLINE chip shows no number, so no surface contradicts); n>0 → `setAppBadge(n)`, n==0 → `clearAppBadge()`, feature-detected (`"setAppBadge" in navigator` — TS 5.6.3 lib.dom already types it) + .catch-silenced (rejects when not installed); badge lives only while the installed app window runs — no push machinery
- **Scope:** zero new deps, zero schema/API/git-call changes, no new localStorage keys; backend diff = version.py + main.py static routes only; the Installation_Guideline/Onboarding_Prompt need NOTHING (no cross-repo contract change)

## v0.1.11.0 — Rings & Constellations (2026-07-28)

**Key Highlights**

- Ninth harvest cycle (Apple-rings class · chord/dependency-wheel class simplified to an arc diagram); the FIRST all-frontend release (backend/schema/API/hook untouched — `git diff` touches Backend only at the version constant); plan passed a 10-pass CDD review loop (5 findings RV1-RV5, 5/5 consecutive clean) with the constellation math dry-run-proven on the LIVE coupling pairs (12 nodes, deterministic hub ties, buckets matching the badge rule) and the fresh-UTC-day 0% rings confirming rollover semantics on real data
- **Daily goal rings** (D1/B.1): `goalRings.tsx` — the 2-circles-per-ring technique (dim track + progress with fixed dasharray/animated dashoffset, linecap round, rotate -90; r 50/38/26, stroke 10; module-private teal/sky/amber palette), values from `calendar[last]` (the UTC-today basis), goals in localStorage `katlab.goals` (defaults {30, 120, 2}; per-field try-guarded fallback; INVALID edits REJECTED — the field keeps its previous value, never coerced — RV4); visual cap at 100% with TRUE uncapped legend numbers; inline dashoffset transition gated by `prefersReducedMotion()`; ring-close burst = the celebration recipe WHOLE + the timeout id cleared on unmount (RV5 — the component REMOUNTS by design); crossings fire on STATS changes only (RV2/RV3: the rings instance — and ONLY it, never OverviewView — is keyed by scope so tab switches reseed via the first-payload rule; goal edits can never false-fire BY CONSTRUCTION — prev stores raw values and the compare reads the goal fresh, so a lowered goal never satisfies `prev < goal` — CFT-1); ⚙ inline settings popover (not an overlay — the palette stays live; pointerdown-outside closes); the identity/churn row became lg:grid-cols-3 with rings FIRST
- **Coupling constellation** (D2/B.2): `couplingArcs.tsx` — nodes on a baseline ordered by total shared-weight (ties alpha), semicircle arcs with strokeWidth 1+3·(shared/couplingMax) and stroke = EXACTLY the list badge's bucket expression (`couplingMax` arrives as a PROP from OverviewView's one computation — RV1, never recomputed); node dots + mid-truncated basename labels, full-path tooltips, every node a FileStory opener; fixed 400x150 viewBox + width 100%; the coupling card gained the `list | arcs` toggle (the flat|city recipe verbatim, DEFAULT arcs, persisted `katlab.couplingView`) with the list view byte-identical
- **Rings on the wall** (D1/C.1): `GoalRings compact` in focus mode between the in-flight feed and the today-line (element 7; legend/gear hidden; same localStorage goals; bursts on the visible wall; no scope key needed — the wall scope is snapshotted)
- **Scope:** zero backend, zero new deps, zero new endpoints/git calls; two new localStorage keys; the Installation_Guideline/Onboarding_Prompt need NOTHING (no cross-repo contract change)
- **Post-implementation CFT loop** (CFT-1..CFT-5, 9 passes ending 5/5 clean — see plan CFT log): the goal-edit false-fire guard documented to its as-built by-construction mechanism across FOUR homes (plan, code comment, Version_Notes, Architecture_Notes — the plan's "reseed" wording was the stale one; the code was always right), one 21-line indent fix; executed batteries beyond the fixes — 7/7 hostile inputs (primitive/array/string/float/malformed goals JSON, empty calendar, single-pair arcs), live-data E2E on the real payloads (10 arcs / 12 nodes matching the review dry-run), and the byte-identical-bundle proof (the rebuild after comment-only edits yielded the same content-hashed name)

## v0.1.10.0 — Identity & Focus (2026-07-27)

**Key Highlights**

- Seventh research harvest (the three seeds backlogged 2026-07-25: onefetch-class identity card · Git-Heat-Map-class treemap · kiosk-class ambient wall); plan passed TWO CDD review loops — 13 passes, then Amendment A1 (user-ruled coupling retrofit) + an 11-pass post-A1 re-review — 12 findings RV1-RV12 fixed in place, both loops 5/5 consecutive clean, with the identity/churn aggregations, the squarify layout (Bruls canonical fixture: worst aspect 2.78 vs 16.0 naive) AND the A1 blast radius (102->99 pairs, all 3 gone pairs plan-involving) dry-run-proven on the REAL DB pre-implementation
- **Repo identity card** (D1/A.1/B.1): `/api/stats` gains `identity` — top-8 lowercase basename extensions by (-count, ext) + ext_total (churn-weighted EVENT counts; "" bucket labelled "(no ext)"), sessions (distinct non-null), first_event_ts, ALL-TIME commits; fixed shape BOTH paths; `identityCard.tsx` renders a hand-rolled stroke-dasharray donut (module-private 8-step categorical palette by rank, slate "other" remainder) + the facts line (effort = the client calendar-minutes sum, "(365d, UTC)" labelled); ext_total 0 swaps the DONUT AREA only (RV4 — the facts line stays)
- **Churn treemap** (D2/A.1/B.2): `file_churn` — top 20 of the SAME new file-level GROUP BY by (-events, repo, file); `churnMap.tsx` — hand-rolled squarify (grow-while-worst-aspect-improves, ~40 lines, canonical-fixture-proven), FIXED 400x180 viewBox + width 100% (RV7 — layout + the >=60x14 label fit rule in viewBox units), tile fill = recency bands (<24h/<7d/<30d/older -> RAMP[4]/[3]/[2]/[1], the shared ramp), SVG-child tooltips with raw ISO, every tile opens the EXISTING FileStory; card hidden at 0 rows
- **Ambient focus mode** (D3/C.1): `focusMode.tsx` — frontend-only z-50 wall (zero fetches; renders App's live WS state): giant status hero (repo CLEAN ✓ / N uncommitted; ALL x/y clean + chips), minute clock (own 60s interval, cleaned), the "in flight" feed = newest 6 UNCOMMITTED events of the SNAPSHOT scope (RV1/RV8 — App.events IS the uncommitted pool; commits drain it to "All clear ✨"), session pulse, "≈Xm today (UTC) · 🔥 streak" line, Skyline strip; scope SNAPSHOT at mount (RV3) + focus TRAPPED on ✕ (RV6, the palette trap recipe) + the GraphShell open-effect recipe (RV5 — Esc listener + body flag both cleaned in teardown) + focus restore; palette entry "Enter focus mode" + a Legend line; Esc/✕ only — NO click-out (deliberate)
- **Amendment A1** (user 2026-07-26): the RV2 DOUBLE exclusion (tasks set OR the documented PLAN_*.txt basename) became ONE shared `_is_plan_file` helper retrofitted into the SHIPPED v0.1.7.0 coupling at BOTH call sites (all-time pairs + wrapped top_pair) — task-less plan files leave the four plan-file-excluding surfaces (coupling, top_pair, identity, churn) while events_per_task and wrapped.files_touched stay deliberately inclusive (the RV10 boundary); live effect: txt 61 -> 4 in the identity mix, zero PLAN_ pairs/tiles anywhere
- **Scope:** two additive read-only `/api/stats` keys fed by ONE new GROUP BY + three scalar aggregates; NO schema change, no new endpoints, no new git calls, zero new deps (package.json untouched); hook + event version untouched
- **Post-implementation CFT loop** (CFT-1, 6 passes ending 5/5 clean — see plan CFT log): one log-arithmetic correction (the stats-key total is 8 pre-release + 2 new = 10 — the fixture asserted it correctly, the log had miscounted); executed batteries beyond the fix — the palette seam source-proven (run-then-close, Ctrl+K suppression under the overlay flag), a fresh contiguous read of the three git-diff-invisible new components, a 6/6-equivalent identityCard render battery closing the last by-construction gap, a hostile-input battery through the real `get_stats` (root-level plans, control-char filenames, pre-1970 TEXT timestamps — all safe), and a LIVE-DATA end-to-end (the real bundled ChurnMap over the actual 8100 payload: 20 tiles, areas summing exactly to the canvas, every tile plan-free; the live `wrapped.top_pair` now a genuine code-code pair — the A1 effect visible on the shipped surface)

## v0.1.9.0 — Skyline & Arcade (2026-07-26)

**Key Highlights**

- Sixth research harvest (gh-skyline/isometric-contributions class · ryo-ma/github-profile-trophy · hoovercj/vscode-power-mode · mermaid built-in gitGraph); plan passed a 12-pass CDD review loop (11 findings RV1-RV11, 5/5 consecutive clean) with the graph builder fixture-tested against the REAL bundled code and the %P parse proven on real repos (TM root/HEAD + EA/UM merge commits) pre-flight
- **Isometric skyline** (D1/B.1): the parked R-X centerpiece as PURE SVG (2:1 dimetric; ISO_X 7 / ISO_Y 3.5 / HMAX 40, sqrt heights min 3, back-to-front (wi+di) painter order) — top faces = the calendar's exported RAMP via the LIFTED `rampBucket(n, max)` (single ramp+bucket source), side faces module-private darkenings, commit dots + flat-identical tooltips; `flat | city` header toggle (DEFAULT city, persisted "katlab.calendarView"); the legend LIFTED to an exported `<RampLegend>` rendered once by the card below whichever view (RV8)
- **Trophy case** (D2/C.1): `trophies.tsx` — 7 ranked trophies (Collector/Streak Keeper/Shipper/Night Owl/Weekend Warrior/Marathoner/Finisher; rank = thresholds crossed, — /C/B/A/S/SS/SSS, >= semantics, progress bar to next) + 2 SECRETS (Century Day, Six of Six — "???" until unlocked); frontend-only from existing stats/tasks props, scope-aware re-ranks; window-based trophies decay honestly with the labelled [365d] basis; no unlock toasts/OS alerts (attention discipline)
- **Live capture combo** (D3/C.2): WS `event_resolved` increments (ONE push per ingested event, live-only — startup catch-up pushes no WS, source-verified); count + lastMs in REFS mirrored to state (RV7 — the handler's closure is mount-time stale for state), decay via the mirrored `EFFORT_GAP_MAX_MIN` (one effort block = one combo); chip "🔥 ×N" left of the bell at >= 3, render-derived active (pulse recipe: WS on, P8 tick expires); milestones {5,10,25,50,100,250} fire a NEW `combo-pop` keyframe + ~8 `.burst-p` particles with the celebration's ~900ms nonce-compare clear (RV11), skipped hidden/reduced-motion; ephemeral (reload resets — a flow meter, not a stat)
- **Commit graph** (D4/A.1/B.2): `commit_info`'s format gains `%P` on the ONE existing `git show` call (zero new git invocations; every writer inherits — detect/backfill/sweep); `commits.parents TEXT` via the migration loop GENERALIZED to a per-table map (pre-upgrade rows NULL beyond the last-20 backfill window, which self-heals on the first restart — RV9); `/api/history` flows it free (`dict(row)` over `SELECT *`); `mermaidGraph.ts` gains `buildGitGraph` (first 20 of the page, main line = page order on the SANITIZED `mainBranchName` — never literal "main" (RV2); merges render SOLELY via the decoration with the side tip "*"-suffixed (RV4 — the tip usually also sits on the main line) and M's id on the merge statement (RV5); octopus parents beyond #2 stay list-only (RV6); NULL/"" -> plain chain; empty page -> def null) + `renderGitGraph` sharing the ONE initialize (now with gitGraph themeVariables); History "⎇ graph" expand card via the GraphPanel effect recipe VERBATIM (alive flag + try/catch note + busy — RV10) + DOMParser+adoptNode injection, mermaid chunk still loads only on first expand
- **Scope:** ONE additive schema column (first schema touch since v0.1.6.0) + one additive `/api/history` field; no new endpoints, no new git calls, zero new deps (package.json untouched); hook + event version untouched
- **Post-implementation CFT loop** (CFT-1..CFT-2, 6 passes ending 5/5 clean — see plan CFT log): the gitGraph `checkout` now QUOTES the sanitized main-branch name (a detached-HEAD repo's main is the v0.1.6.0 short-hash fallback — digit-led — and the grammar's bare REFERENCE token requires a leading letter; parse-proven both ways against the DOM-free `@mermaid-js/parser`); plus a cosmetic re-indent from the legend lift; executed batteries beyond the fixes — 19/19 node render-tests over the three new components (skyline/flat tooltip + bucket parity, painter order, trophy `>=` boundaries, secret lock/unlock, combo gating), a 6/6 hostile-input battery, and a LIVE-DATA build+parse of all three repos' actual history rows (real SLASHED branch names exercised the sanitization on production data)

**Key Highlights**

- Fifth research harvest (gource/Gource-Web replay class · ActivityWatch timeline · git-wrapped/GitHubUnwrapped recap class); plan passed a 26-pass CDD review loop (22 findings RV1-RV22, 5/5 consecutive clean) with the wrapped algorithms dry-run VERBATIM against the real DB pre-implementation; ONE honest implementation drop (IMPL-1): the proposed KPI count-up (R-W) already shipped in v0.1.4.0 (`useCountUp` in OverviewView, T5 entrance + RV8-v0.1.6.0 formatter-per-frame stand) — dropped by user decision, never claimed
- **Day lanes** (D1/A.1/B.1): `/api/events` gains optional `since`+`until` (ISO-Z TEXT compare, `>= since AND < until`, composes with every filter; F38 untouched); `dayLanes.tsx` pure-SVG 24h LOCAL lanes — lanes derive from the FETCHED rows' repo_ids (RV19 — offline repos' history still renders), blocks = presentation-only clusters via the mirrored constants (RV4-v0.1.6.0 contract), session-colored dots, local-span tooltips; day picker (today default, forward-disabled, month roll-over free); fetch is repo-scoped (RV17) + windowed local-day→UTC + ≤3×500 pages with the per-effect alive flag (RV16) + ts-ASC sort (RV8) + "(fetched window)" honesty; FRESHNESS: stats-prop changes refetch TODAY only (past days immutable), never while replay is armed (RV9)
- **Activity replay** (D2/B.2): in-card engine — ONE rAF loop, virtual time from CLAMPED accumulated dt (hidden tab pauses, never skips; 1x = day/30s, 2x/4x), amber SVG cursor, 8-chip feed (time·file·mode badge, repo prefix on ALL), "N events · ≈Xm" via the mirrored constants over consumed rows (RV6), range-input scrubber with binary-search seek; exits: ✕, day-switch (RV9), scope-switch (RV21 — OverviewView persists un-keyed), unmount cancels the rAF (RV15); reduced motion = no auto-play, manual scrub stays
- **Weekly wrapped** (D3/A.2/C.1): `/api/stats` gains `wrapped` — days = the calendar's last-7 projection (RV4), top_task via window epochs + `_cluster_minutes` (tie `(-minutes, repo, task_ref)` — RV5), busiest_hour via the punch-card local mapping + the v0.1.7.0 CFT-1 guard (tie `(-events, dow, hour)`), top_pair = the v0.1.7.0 coupling algorithm window-restricted, files_touched (plan files INCLUDED — RV10), commits; FIXED shape both paths, nullable sub-objects (`_empty_wrapped`); `WrappedCard.tsx` modal — STATIC by init-once snapshot (RV22), title client-resolved via the tasks prop (RV11, short-ref fallback), day label via the EXPORTED punchCard DAYS (RV5), streak via imported streakOf; "Your week ✨" Overview button + palette action; empty-week friendly state
- **Scope:** NO schema change; two additive `/api/events` params + one additive `/api/stats` key; no new endpoints, no new git calls, zero new deps (package.json untouched)
- **Post-implementation CFT loop** (CFT-1..CFT-2, 7 passes ending 5/5 clean — see plan CFT log): the replay cursor now paints AFTER the lanes (it read dimmed under the semi-transparent blocks exactly where events cluster); the wrapped mini-bar tooltip fixed from an inert SVG-style `<title>` child to the HTML `title` attribute; plus executed parity sweeps (replay-counter mirror vs `_cluster_minutes` on full arrays and consumed prefixes, window battery with the pre-1970 poison guard, docs-vs-code claims)

## v0.1.7.1 — Sweep Hotfix F58 (2026-07-21)

**Key Highlights**

- **F58** (`watcher.py` `_poll_loop`): gitignored-file edits (detailed plans under ignored `temp/`) accrue UNLINKED events WITHOUT dirtying the repo — none of the three sweep triggers (startup / commit-transition / poll dirty→clean, all gated on restart or a status CHANGE) ever fires, so the event lingers as "uncommitted" until an unrelated restart/commit (first hit: UM_Dev id 675, a plan-log edit during a sustained clean stretch; 91 more accrued before the fixed backend was loaded — self-healed by the first post-restart poll)
- Fix: an UNCONDITIONAL safety-net sweep outside the transition gate — every poll, `clean AND has_unlinked_events` → `_sweep` to HEAD with `swept=1`; correct by ordering (`_detect_commits` links committable events first in the same iteration → only truly-unlinkable events remain), idempotent, offline-safe, `LIMIT 1` probe cost when idle; WS push reuses the existing `commit_detected {swept: true}` shape
- **Scope:** one file + the version constant; no schema/API/hook/frontend change

## v0.1.7.0 — Story & Delight (2026-07-20)

**Key Highlights**

- Third harvest from the external research passes (GitHub punch-card/streak conventions · CodeScene-class change coupling · celebration/toast UX · View Transitions API); plan passed a 19-pass CDD review loop (15 findings RV1-RV15, 5/5 consecutive clean) with BOTH new algorithms empirically dry-run against the real DB pre-implementation (coupling surfaced the EA `katlab_lib_service` trio; the punch-card weekday mapping proven on 668 live events, Sunday row 72%)
- **Change coupling** (D1/A.1/B.1): `/api/stats` gains `file_coupling` — DISTINCT (repo, task_ref, file) triples → per-task file sets → unordered pairs; plan files excluded via `tasks.plan_file`, >30-file mega-tasks skipped, top-10 by `(-shared, repo, file_a, file_b)` (deterministic ties); Overview card hidden entirely at 0 pairs; middle-truncated file names, each a FileStory opener
- **File story** (D2/A.2/B.2): `/api/events` gains the optional `file` param (exact match, composes with `repo`); `FileStory.tsx` modal per the SessionTimeline recipe — static snapshot, ≤3×500 pages, ts-ASC client sort, UTC DATE separators (day boundaries beat gap markers for a file's life), header `N events · first → last · N commits · ≈effort` via the mirrored constants, "(fetched window)" when truncated; openers = Changes task-group file names ONLY (the v0.1.5.0 RV4 zone precedent — History/queue stay plain) + both files of a coupling row; branch shown per the v0.1.6.0 differs-suffix rule
- **Punch card** (D3/A.1/B.1): `/api/stats` gains `punch_card` — 7×24 counts, Sunday-first rows via `(weekday()+1)%7` (the RV2 Python-Monday trap), SERVER-LOCAL hours ("when do I work" is a wall-clock question; tooltips say "(local time)"); pure-SVG `punchCard.tsx` sharing the calendar's exported RAMP (RV7 single ramp source); both new stats keys fixed-shape in BOTH `get_stats` return paths (the RV28 rule)
- **Streak** (D4/C.1): `streakOf()` exported beside the calendar component (single home) — consecutive events>0 UTC days ending at the last calendar day, with the GitHub today-still-zero grace (ends at yesterday); "🔥 N-day streak" in the calendar card header only when ≥ 2 (1-day = noise)
- **Session pulse** (D5/C.2): breathing teal dot before the heartbeat text while `now - last_event_ts < 5min` — derived AT RENDER (last_event_ts over activity_buckets: buckets linger stale between fetches); WS syncs turn it on promptly, the P8 60s tick expires it; CSS keyframes, reduced-motion → static dot
- **CLEAN ✓ celebration** (D6/C.3): `notifyStatusChange()` now RETURNS the dirty→CLEAN transition (the RV9 prevClean map stays the one owner; OS behavior unchanged, hidden-only); channel matrix per transition (RV1): persistent toast ALWAYS (the durable record — bottom-right z-30 BELOW every overlay, one per repo with replace, dismissed by ✕ or any outside pointerdown, NOT an overlay so Ctrl+K stays live), ~12-particle chip burst while VISIBLE (800ms hand-rolled CSS spans, nonce-keyed {repo, n}, skipped under reduced motion / when the chip isn't rendered), OS notification while HIDDEN; re-dirty→re-CLEAN fires again
- **View Transitions** (D7/C.4): `withViewTransition()` in theme.ts — feature-detect + NOT reduced-motion → `startViewTransition(() => flushSync(update))` (react-dom, no new dep), else plain update; applied at the repo TabButtons, the three view TabButtons and `navigateToRepo` ONLY (filters/palette stay instant — deliberate); default browser crossfade, zero custom CSS
- **Scope:** NO schema change (first release since v0.1.4.0 — hook, event version, DB all untouched); two additive read-only `/api/stats` keys + one optional `/api/events` param; no new endpoints, no new git calls, zero new deps (package.json untouched)
- **Post-implementation CFT loop** (CFT-1..CFT-4, 14 passes ending 5/5 clean — see plan CFT log): punch-card bucketing hardened against hand-crafted pre-1970 timestamps (Windows `fromtimestamp` OSError would have 500'd `/api/stats`; bucket-only skip, effort/calendar unchanged); demo generator never emits future events + the day-0/streak claims carry the honest first-5-UTC-minutes boundary; modal `onClose` identities pinned with `useCallback` (the overlay effect no longer re-runs on tick/WS re-renders — keyboard focus stays in an open story/timeline); gate echo sweep aligned every normative home

## v0.1.6.0 — Effort & Context (2026-07-19)

**Key Highlights**

- Second harvest from the 2026-07-19 research pass (WakaTime/Wakapi · GitDeck · agents-observe); plan passed a 22-pass CDD review loop (25 findings RV1-RV25, 5/5 consecutive clean) with two claims EMPIRICALLY corrected pre-implementation (git ascends past the demo's marker .git; the effort algorithm dry-run against the real DB)
- **Effort attribution** (D1/B.1/C.1): block clustering over existing capture timestamps (GAP 15min + TAIL 2min, `minutes = round(s/60)`, ONE algorithm home in `db.py` mirrored as display constants in `theme.ts`); `/api/stats` gains `effort_per_task` (top 10, with distinct-session counts) + per-day `minutes` on the calendar — fixed shape in BOTH return paths; stats fetch LIFTED OverviewView→App (sidebar/groups need it on the Changes view); surfaces = sidebar cards, group headers, "time today (UTC)" KPI (day-basis labelled — digest KPIs are otherwise local-day), calendar tooltips (only when > 0), digest KPI (reads the same value, never re-clusters); every render ≈-prefixed via one `fmtMinutes()`
- **Branch awareness** (D2/A.1/A.2/B.2/C.2): hook reads `.git/HEAD` stdlib fail-open (dir + worktree-file cases, relative gitdir resolved) → `events.branch` (EVENT VERSION STAYS 1, third additive-optional-key repetition; non-empty-str-or-None guard both ends); migration generalized to a needed-columns loop (covers v0.1.4.0-era DBs); `git_module.current_branch()` (read-only rev-parse, detached→short hash) → status-bar `⎇` chip (branch always present in the status dict, per-field keep-last-known); EventRow suffix ONLY when event-branch ≠ current-branch; demo shows the ENCLOSING repo's branch (git ascent — documented)
- **Session timeline** (D3/C.3): `/api/events?session=` + a static-snapshot modal (chip "timeline" button + palette action while a session filter is active) — cross-repo rows sorted ts-ASC, task-transition separators, gap markers past the mirrored 15-min constant, truncation footnoted and the header effort labelled "(fetched window)" when partial
- **Bell v2 rhythm nudges** (D4/C.4): two new badge-counted rows — "in-progress idle ≈ Nh/Nd" (`tasks.last_event_ts`, threshold 24h) and "uncommitted for ≈ Nh/Nd" (`repos.oldest_uncommitted_ts`, exact MIN, threshold 48h); ages via a new `fmtAge()` (fmtRel switches to absolute past 24h); null fields never nag; thresholds are frontend constants this release
- **Scope:** all backend additions additive-only; zero new deps (package.json untouched); no new endpoints; one extra read-only `rev-parse` per status refresh (two while HEAD is detached — the short-hash fallback)

## v0.1.5.0 — Insights & Awareness (2026-07-19)

**Key Highlights**

- First harvest from the 2026-07-19 external research pass (disler multi-agent observability · GitDock · Git-Heat-Map family); plan passed a 23-pass CDD review loop (32 findings RV1-RV32 fixed, 5/5 consecutive clean) before implementation
- **Session attribution** (D1/A.1-A.2/C.1): the hook captures `session_id` (already on PostToolUse stdin, previously dropped) — EVENT VERSION STAYS 1 (optional key, upgrade-order-free both directions; watcher hard-rejects other versions); `events.session_id TEXT` via idempotent PRAGMA→ALTER migration in `init_db`; non-empty-str-or-None type guard at BOTH ends (a non-string bind would wedge the F13 insert+offset transaction); UI identity dots (`theme.ts sessionColor`, same session = same color everywhere) + click-to-filter in Changes only (App-level state, repo-aware task-filter reset: a matching-repo tab switch now KEEPS the X4 filter — intentional change); MODE_BADGE lifted App→theme.ts (single label source for App + digest)
- **Calendar heatmap** (D2/B.1/C.2): `activity_calendar` on `/api/stats` — 365 zero-filled UTC days {day, events, commits} in BOTH `get_stats` return paths (fixed shape incl. the empty-scope early return); pure-SVG Sunday-first grid (user pick), quartile teal ramp, commit corner dots, (UTC) tooltips, month labels at 1st-of-month weeks, overflow-x-auto on narrow viewports; behind the charts' totalEvents===0 gate
- **Discipline guard** (D3/C.3): frontend-only — armed = repo has parsed tasks; violation = in-progress ≠ 1 → amber "⚠ N active" status-bar chip + live banner armed by WS queue-landers (UNKNOWN/AMBIGUOUS) and rendered only while STILL violating (task_updated re-sync clears live); banner is live-only (startup catch-up pushes no WS)
- **Attention bell** (D4/C.4): ONE header bell + actionable badge (picks pending / discipline / OFFLINE / no-capture-yet; uncommitted informational); Σ(rows) == ALL-tab KPI == queue (scope-matched invariant); row click navigates with the DEFERRED post-mount scroll (a synchronous scrollIntoView after setView finds no element); footer hosts the OS-alerts toggle (user: one bell)
- **OS notifications** (D5/D.1): `notify.ts`, localStorage-persisted, hidden-tab-only; 3 WS triggers (pick-needed coalesced 5s/repo, dirty→CLEAN via a ref-map — never from inside a setRepos updater (StrictMode double-invoke), warnings); per-repo+kind tags; any non-granted permission (denied OR dismissed) snaps off
- **Daily digest** (D7/D.3): `digest.ts` client-side Blob `TrackingMonitor_Digest_<date>[_<repo>].html` — local-day scope, KPI strip + repo→task→file breakdown (badges via lifted MODE_BADGE + session dots), ≤3 pages with explicit truncation footnote, fetch failure aborts with an inline note (never a partial file), fonts via link+fallback, no external JS
- **Ctrl+K palette** (D6/D.2): hand-rolled subsequence fuzzy, combobox/listbox ARIA, focus trap/restore; suppressed while the Mermaid overlay is open (body data-attribute); groupMode lifted ChangesView→App; cross-tab task picks survive the repo-aware filter reset; tree-toggle lands on Changes and the alerts action opens the panel (visible-effect rule)
- **Scope:** backend additions are additive-only (schema column + one stats field); zero new deps (package.json untouched); demo emits 3 deterministic session ids across all 55 generated lines
- **Post-implementation CFT loop** (CFT-1..CFT-9, 14 passes ending 5/5 clean — see plan CFT log): ingest now validates the whole event-line shape — non-dict JSON lines and non-string ts/file warn+skip with the offset advancing, tool coerces to "?" (hook mirrors it) — F26's never-stall holds for ANY hand-crafted line; deferred pick-queue scroll made state-based (fires in every path, incl. same-view palette/notification jumps); demo session ids distinct in their first 8 chars; calendar right-pad + digest font-weight parity

## v0.1.4.0 — Visual Polish (2026-07-17)

**Key Highlights**

- Second harvest from `nicobailon/visual-explainer` — the POLISH layer on v0.1.3.0's visuals. Discovery ran loop-until-dry (4 passes, 2 consecutive dry); the plan passed TWO CDD review series (S1: 16 rounds, 12 findings; S2 post-decisions: 22 rounds, 10 findings; each ending 5/5 consecutive clean) before implementation
- **Typography** (D3/A.1): real pairing — "Plus Jakarta Sans" (UI) + "Azeret Mono" (mono; v-e's canon match, user-picked) via Google Fonts `<link>` (display=swap, offline-safe fallback stacks) wired into Tailwind `fontFamily`; the Mermaid graph (`themeVariables.fontFamily`) AND Chart.js (`defaults.font.family` — canvas text ignores page CSS) adopt it too
- **KPI metric cards** (D1/B.1): six "at a glance" numbers above the charts — captured events, auto-attributed %, need-a-pick (sourced from the UNCOMMITTED events prop so it always EQUALS the manual-pick queue N), repos clean x/y, uncommitted changes, busiest task — scope-aware, empty-safe (no NaN), reduced-motion-safe rAF count-up; over-wide hero values transform-scale so huge counts never break the auto-fit grid (D2/B.2)
- **Mermaid diagram shell** (D7/B.4): the static injected SVG becomes a real viewport — +/− zoom (clamp ~0.08-6.5, step ~14%), drag-pan, ctrl/cmd+wheel zoom-at-cursor, keyboard +/−/0, smart-fit + ResizeObserver re-fit, fullscreen-expand overlay (Esc / click-out); SVG injected via DOMParser+adoptNode (not raw innerHTML); dashed `-.->` edges mark the uncommitted sink + a one-line shape legend
- **Changed-files tree** (D6/B.3): "by task | by folder" toggle on the Changes view — per-repo monospace ├──/└── tree of the uncommitted files with per-leaf edit-count badges (churn hotspots); the pick queue is never hidden (P11) and a task filter never subsets the tree (per-repo by definition)
- **Sticky scroll-spy nav** (D4/C.1): sticky mini-TOC over the Changes view ("manual pick (N)" + task ids) with IntersectionObserver highlight + smooth scroll; hidden at ≤1 group
- **Staggered reveal** (D5/D.1): cards/groups fade-lift in ONCE per view per session (view remounts never re-stagger); `prefers-reduced-motion` ⇒ everything visible immediately, no motion anywhere (reveal + count-up)
- **Background atmosphere** (D8): faint radial glow + dot grid from the border color — fixed, zero scroll cost
- **Scope**: UI-only — the sole Backend/ touch is the version constant; no endpoint, no schema, no new git calls

## v0.1.3.0 — Visualization (2026-07-14)

**Key Highlights**

- Inspired by `nicobailon/visual-explainer` (the tracker was all text/lists); adds real visuals. Plan passed a 33-pass CDD review (26 findings R1-R26; all 4 novel algorithms prototype-proven on live data) before implementation
- **Colored diff** (D1): the raw `<pre>` becomes a GitHub-style colored diff — position-aware classification (headers only pre-hunk; in-hunk first char = +added/-removed/context), so even an added line whose content starts with `++ ` colors correctly; honest empty-state messages (gitignored/untracked/no-changes/not-in-commit) render as italic notes, not fake diffs
- **Overview dashboard** (D2, Chart.js): attribution-health doughnut (events by mode) + events-per-task bar (top 10, `(repo, task_ref)`-keyed, unresolved excluded) + 14-day activity line; ALL scope aggregates **configured repos only** (a removed repo's lingering events never pollute it); charts refresh on real syncs, not ticks
- **Capture sparkline** (D3): a pure-SVG 60-minute activity spark in each status-bar chip — "is the hook firing?" at a glance; fed by `activity_buckets` on `/api/repos`
- **Relationship map** (D4, Mermaid, lazy-loaded): per-plan task→commit backbone (file counts on task nodes, uncommitted sink) built from `/api/tasks` + `/api/history`; ≤12-node cap with a hybrid fallback for huge plans; mermaid@11 is dynamically imported (its own Vite chunk — the initial bundle stays lean)
- **One color source** (D6/R26): `theme.ts` `MODE_COLOR` (hex) drives both the badges (inline style) and the doughnut — a mode looks identical everywhere, no Tailwind-class↔hex drift
- **Backend**: one additive read-only endpoint `/api/stats` (zero-filled fixed shape: all 6 modes, 14 UTC days) + `activity_buckets` on `/api/repos`; zero new git calls, no schema change

## v0.1.2.0 — UI/UX Enhancement (2026-07-13)

**Key Highlights**

- Driven by 5 user findings (UF1-UF5, 2026-07-12) after the first real multi-repo day + 4 approved extras (X1-X4); plan passed a 13-pass CDD review loop (15 findings P1-P15 fixed, 5/5 consecutive clean) before implementation
- **UF1 Terms/states**: badges relabeled to human terms with technical names in tooltips (Declared/B, Active task/A_SCOPED, Active task */A_GLOBAL, Pick: multi/AMBIGUOUS, Pick: none/UNKNOWN, Your pick/MANUAL, auto-linked/swept) + header "?" Legend popover; task chips gain derived commit-aware states: `done ✓` vs `done, uncommitted` + per-task uncommitted counts
- **UF2 Diff everywhere**: `/api/diff?commit=` serves per-commit diffs via `git show --format=` (header suppressed); empty HEAD-diffs classified honestly (gitignored / untracked / no changes — prefix-parsed `git status --porcelain --ignored`, P1: ignored DIRS reported, not files); swept-event commit diffs get an explanatory message; History rows now have diff toggles
- **UF3 Sidebar**: tasks grouped by plan file with x/y-done rollups; Active/Done sections (Done collapsed, behind the All chip); finished+committed plans stop cluttering the list
- **UF4 Timestamps**: local `YYYY-MM-DD HH:mm:ss` + relative times ("3m ago") with raw ISO in tooltips (`Frontend/src/format.ts`); 60s re-render tick so idle UIs never freeze; API stays ISO-8601 UTC-Z
- **UF5 Theme**: one hue per mode, 12px base rows, higher-contrast secondary text, colored section headers, hover/focus states
- **X1** bulk-assign manual picks (select-all + one dropdown + candidate-safety + inline result note) · **X2** plan-file edit noise collapsed to one expandable line per task (exact plan_file-set match — covers EA_Dev's temp/<Component>/ layout) · **X3** capture heartbeat per repo ("last capture 3m ago" in the status bar, `last_event_ts` in /api/repos) · **X4** click a task to filter the Changes view (manual-pick queue exempt)
- Zero schema changes; git usage stays strictly read-only (`show --format=`, `status --porcelain --ignored` added to the same allowed set)

## v0.1.1.0 — User-Scope Capture (2026-07-12)

**Key Highlights**

- **Root cause fixed**: the real workflow is ONE Claude Code session (VS Code multi-root, rooted in EA) spanning ALL repos, but Claude Code loads project hooks only from the SESSION root — per-repo hook registration (old guideline Step 1) never fired for the other repos. Verified 2026-07-12: capture was DEAD in every repo (UM had an inert entry; EA had none; both EA `.katlab_tracking/` dirs existed empty from a real server run — server fine, capture dead)
- **Capture**: hook registration moved to ONE user-scope entry (`C:\Users\ADMIN\.claude\settings.json`) — fires in every session, any root; the hook already routed events per edited file (F1), so only registration scope changed. Per-repo registration RETIRED (double-fire risk)
- **Allowlist** (`Hook/katlab_tracking_hook.py`): captures ONLY repos in `Config/repos.yaml` (stdlib regex read; `KATLAB_TRACKER_CONFIG` override; unreadable/empty registry = fail-OPEN capture-all, durability first; supersedes stray-dir note F37). R1 convention: registry `path:` lines stay single-line + single-quoted — the server reads YAML, the hook reads regex
- **UM_Dev registered** as the 3rd monitored repo (`temp/Plan/PLAN_*.txt`; already authoring enhanced-format plans — PLAN_v0.4.3.4 H.1..H.5; older legacy plans parse to zero tasks, harmless)
- **Onboarding contract rewritten** (`Docs/Installation_Guideline.md` + `Guidelines/Onboarding_Prompt.txt`): per-repo steps shrink to legacy-hook cleanup + gitignore + plan rules; registration (user) strictly BEFORE smoke test (allowlist); ordered migration FOLLOW-UPS (EA gitignore BEFORE session restart — prevents tracker self-polluting EA git status)
- **Plan review discipline**: PLAN_v0.1.1.0 passed a 9-pass CDD review loop (4-step methodology, 11 findings fixed, 5 consecutive clean) before implementation
- Verification: V1 hook unit 5/5 (allowlist/fail-open/default-path/malformed-stdin on temp repos), V2 settings merge (all keys preserved), V3 server restart + 3 repos live, V5 guideline dry-read. V4 (real cross-repo smoke) pending user-side follow-ups: EA gitignores → UM legacy-entry removal → workspace session restart → smoke

## v0.1.0.0 — Foundation (2026-07-07)

**Key Highlights**

- Initial release: full vertical slice per `temp/Plan/PLAN_v0.1.0.0_TrackingMonitor_Foundation.txt` (51-pass CDD review, 57 findings fixed before implementation)
- **Capture**: PostToolUse hook (`Hook/katlab_tracking_hook.py`) — repo-root discovery via `.git` ancestor walk (F1), forward-slash normalization (F15), exit-0-always, stdlib-only
- **Enhanced plan format**: `Docs/Plan_Format_Spec.md` — column-0 `<task>` blocks (F4), required/optional tag matrix (F53), CRLF/LF + UTF-8/BOM tolerant (F50/F51), warnings for absolute patterns (F52)
- **Server**: FastAPI single process — fixed startup order (F24: plans before catch-up), exactly-once ingest (F13), offset truncation reset (F5), malformed-line skip (F26), atomic plan→task sync (F11/F16), sweep chain for rename/delete + down-across-commit cases (F9/F32/F35/F36), transient-git failure isolation everywhere (F41/F42), two-tier config errors (F14/F43/F44), periodic status poll for non-Claude edits (F22)
- **API**: REST (UM envelope, paginated F38) + WebSocket (event_resolved/task_updated/repo_status_changed/commit_detected/warning — F47)
- **UI**: React+Vite+Tailwind (English) — dynamic repo tabs + ALL (F31), status bar CLEAN/N/OFFLINE (F46), task sidebar, changes-grouped-by-task with task-ref links (F48) + diff viewer (F21 staged-visible), AMBIGUOUS/UNKNOWN manual-pick queue (F17, zero-task guidance F49), History with load-more (F39) + indirect badges, warnings banner, WS auto-reconnect with REST re-sync (F29)
- **Ops**: `Scripts/start_tracking_monitor.bat` (step-abort F54, first-run frontend build F20, portless F10), fresh-clone-safe dirs (F56/F57/F18)
- **Onboarding**: `Docs/Installation_Guideline.md` — merge-not-overwrite hook registration (F40), restart-after-register (F25), stray-dir note (F37)

**Post-release addition (2026-07-09; moved to `Scripts/Demo/` 2026-07-15)**: DEMO MODE — `Scripts/Demo/start_demo.bat` (+ `stop_demo.bat` / `restart_demo.bat`) generates a self-contained scratch repo under gitignored `Demo/runtime/` (marker `.git` dir, sample enhanced plan, 10 synthetic events covering B / A_SCOPED / AMBIGUOUS / UNKNOWN + 2 intentional warnings) and launches the server with `KATLAB_TRACKER_CONFIG` / `KATLAB_TRACKER_DB` env overrides — zero contact with real repos, demo DB isolated from `data/tracking.db`.

**Verification status**: V0/V1/V2/V3/V10 passed in sandbox at release; post-release CFT loop added LIVE passes for V6 (AMBIGUOUS/MANUAL persistence/F16/F49), V7 (restart resilience/F24/F26/F5) and V8 (multi-repo) — all git-free. Remaining user-driven at onboarding: V4 (commit linking), V5 (full UI E2E via real Claude Code session), V9 (poll on real repo), V11 (down-across-commit sweep). CFT fixes: CFT-1..CFT-11 (see plan Status log).

Onboard THIS repository to KATLAB TrackingMonitor so enhanced plans are tracked.

1. Read these TrackingMonitor sources completely. They are read-only for this task:
   - D:\KATLAB_Reiky\_Development_Workspace\KATLAB_TrackingMonitor\Docs\Installation_Guideline.md
   - D:\KATLAB_Reiky\_Development_Workspace\KATLAB_TrackingMonitor\Docs\Plan_Format_Spec.md
   - D:\KATLAB_Reiky\_Development_Workspace\KATLAB_TrackingMonitor\Docs\Tracking_Discipline.md

2. In THIS repository only:
   - Remove only legacy per-repo KATLAB handlers from `.claude/settings.json` or
     `.codex/hooks.json`; preserve all unrelated settings and valid JSON.
   - Add `.katlab_tracking/` to `.gitignore` if absent.
   - Never add a per-repo TrackingMonitor hook.

3. Use `temp/Plan/PLAN_*.txt` and the exact Enhanced Plan Format for every new plan:
   - column-zero `<task id="...">` blocks;
   - required `<title>` and `<status>`;
   - status exactly `pending`, `in-progress`, or `done`;
   - one precise repo-relative forward-slash path/glob per `<files>` line;
   - exactly one in-progress task while implementing;
   - flip before editing and mark done immediately after completion;
   - never use the `PLAN_*.txt` name for a non-plan file.
   - optionally declare one top-level `<verification>` block; ordinary check IDs
     must exist in the operator-reviewed `Config/checks.json`, while
     `review:cdd@N` / `review:cft@N` are manual review streaks.

4. Merge a short version of those standing rules into THIS repo's `AGENTS.md` or
   equivalent instruction file. Preserve existing content. Include the rule to keep
   the manual-pick queue empty.

5. Do not edit TrackingMonitor configuration, provider user settings, or runtime
   data. The operator owns repo registration, TrackingMonitor restart, user-scope
   hook activation/trust, and the smoke test.

6. Treat Mission as evidence observability only. Keep provider activity
   metadata-only, inspect unassigned evidence, and never claim a green gate proves
   universal correctness. TrackingMonitor does not run checks or commit/push/
   checkout/create/delete branches.

7. Report the exact files changed, the plan locations to use, and each onboarding
   check as pass/fail. Do not run Git mutation commands unless explicitly requested.

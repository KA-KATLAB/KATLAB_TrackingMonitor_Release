Onboard THIS repo to KATLAB TrackingMonitor so my new detailed plans are tracked by its UI.

1. Read these two docs carefully and completely (CDD) — they are the source of truth. You must NEVER edit anything inside the TrackingMonitor repo, read-only:
   - D:\KATLAB_Reiky\_Development_Workspace\KATLAB_TrackingMonitor\Docs\Installation_Guideline.md
   - D:\KATLAB_Reiky\_Development_Workspace\KATLAB_TrackingMonitor\Docs\Plan_Format_Spec.md

2. Execute Installation Guideline Steps 1-3 in THIS repo only (skip any step already done — verify, don't redo):
   - Step 1: register the PostToolUse hook in this repo's .claude/settings.json — MERGE with existing content, NEVER overwrite
   - Step 2: add .katlab_tracking/ to .gitignore
   - Step 3: smoke-test — edit a scratch file, confirm .katlab_tracking/events.jsonl received a JSON event for it

3. From now on, author ALL new plans in the Enhanced Plan Format per Plan_Format_Spec.md. Non-negotiables:
   - <task id="..."> blocks starting at column 0; required: id attribute, <title>, <status>
   - <status> is exactly one of: pending | in-progress | done
   - <files>: one path/glob per line, relative to THIS repo root, forward slashes (* does not cross /, ** does)
   - <why>: free text shown in the UI as the reason for the change
   - Discipline: exactly ONE task in-progress at a time; flip statuses live as you work (the tracker re-parses on save); declare <files> precisely
   - The tracker parses ONLY this format — legacy plan formats are invisible to it

4. Step 5 of the guideline (registering this repo in TrackingMonitor's Config/repos.yaml + restarting the server) is MY job — do NOT do it. Just report back: the exact plan-file location(s) you will use, and each step's pass/fail.

Constraints: no git mutation commands in this repo unless I explicitly ask; stop and ask me ASAP if anything conflicts with this repo's existing rules.
